TOPIC = {
    "id": "3.2",
    "domain": 3,
    "title": "Create and configure virtual machines.",
    "short": "Virtual Machines",
}

TERMS = [
    ("Azure virtual machine", "IaaS compute: you choose an image, size, and region; Azure provisions the machine while you manage the OS and everything above it"),
    ("Azure Compute Gallery", "Repository for your own generalized VM images, used as the source for new VMs instead of Marketplace platform images"),
    ("Network interface (NIC)", "Separate resource attached to a VM that joins it to a virtual network/subnet; survives VM deletion by default"),
    ("VM extension", "Small agent that performs post-deployment configuration on a VM, such as the Custom Script Extension or Desired State Configuration"),
    ("Custom Script Extension", "VM extension that downloads and executes a script automatically after deployment, e.g. to install software"),
    ("Stopped-deallocated", "Power state reached via az vm deallocate / Stop-AzVM: hardware is released, compute billing stops, managed disks are kept (and still bill)"),
    ("Server-side encryption (SSE)", "Always-on encryption of all managed disks at rest in the storage service, using platform-managed or customer-managed keys; cannot be disabled"),
    ("Azure Disk Encryption (ADE)", "Per-VM volume encryption inside the guest OS using BitLocker (Windows) or DM-Crypt (Linux), with keys stored in Azure Key Vault"),
    ("Encryption at host", "Per-VM encryption on the host server itself, covering the temporary disk and host caches that ADE and SSE alone do not"),
    ("Key encryption key (KEK)", "Optional key in the same Key Vault that wraps the ADE encryption key for an extra layer of control"),
    ("Move-AzResource / az resource move", "Metadata operation that moves a VM and its dependent resources to another resource group or subscription; both resource groups are locked during the move"),
    ("Azure Resource Mover", "Service built on Azure Site Recovery that replicates a VM to another region, lets you test, then commits the cross-region move"),
    ("B-series", "Burstable VM family that banks CPU credits while idle and bursts when busy — lowest cost for low baseline workloads"),
    ("D-series", "General-purpose VM family with a balanced CPU-to-memory ratio — the default choice for production web servers and small databases"),
    ("E-series", "Memory-optimized VM family with a high memory-to-CPU ratio for in-memory caches, SAP, and large relational databases"),
    ("F-series", "Compute-optimized VM family with a high CPU-to-memory ratio for batch processing, gaming servers, and analytics compute"),
    ("L-series", "Storage-optimized VM family with high local disk throughput/IOPS for big data and NoSQL workloads like Cassandra"),
    ("N-series", "GPU VM family for machine-learning training, rendering, and video processing"),
    ("Temporary disk", "Local storage on the host (D: on Windows, /dev/sdb on Linux) for page/swap files; contents are lost on deallocate, redeploy, or host maintenance"),
    ("Ultra Disk", "Highest-performance managed disk with independently tunable IOPS/throughput; can only be a data disk, never an OS disk"),
    ("Premium SSD", "SSD managed disk for production; required on all of a VM's disks to earn the single-instance 99.9% SLA"),
    ("Snapshot", "Point-in-time, read-only copy of a managed disk used for backup, export, or creating a new disk/VM"),
    ("Fault domain", "Group of hardware sharing a rack, power source, and network switch; availability sets spread VMs across 2-3 of them against unplanned failure"),
    ("Update domain", "Group of VMs Azure reboots together during planned maintenance — only one update domain at a time, up to 20 per set, default 5"),
    ("Availability set", "Intra-datacenter resilience construct spreading VMs across fault and update domains; 2+ VMs earn a 99.95% SLA"),
    ("Availability zone", "Physically separate datacenter within a region with independent power, cooling, and networking; 2+ VMs across zones earn a 99.99% SLA"),
    ("Virtual Machine Scale Set (VMSS)", "Group of identical VMs from one image that autoscales out and in, up to 1,000 instances with platform images"),
    ("Flexible orchestration", "VMSS mode where instances are full standard VMs that can mix sizes/types — the recommended default for new deployments"),
    ("Autoscale rule", "Metric- or schedule-based trigger that scales a VMSS between minimum and maximum instance counts, with a cooldown between actions"),
    ("Rolling upgrade policy", "VMSS upgrade mode that applies a new model/image in batches with health checks between them, avoiding a full outage"),
]

QUESTIONS = [
    # --- multiple choice ---
    {
        "type": "mc",
        "q": "An admin runs az vm create with only the required parameters and later deletes the VM to save money, but the bill barely drops. Which resources were created alongside the VM and continue to bill independently?",
        "choices": [
            "Nothing — a VM is a single self-contained resource",
            "The NIC, managed OS disk, and any public IP address, which are separate resources not deleted with the VM by default",
            "Only the virtual network, which is always deleted with its last VM",
            "A mandatory recovery services vault and a backup policy",
        ],
        "answer": 1,
        "explain": "A VM is never deployed alone: creation also produces a NIC joined to a VNet/subnet, a managed OS disk, and optionally a public IP and NSG — each a separate resource that lives on (and bills) after the VM is deleted. Deleting only the VM orphans these resources; VNets are not auto-deleted, and no vault is created by default.",
    },
    {
        "type": "mc",
        "q": "A deployment pipeline must install and configure a web server on each new VM automatically, immediately after the VM is provisioned. Which feature should the admin use?",
        "choices": ["Custom Script Extension", "Azure Resource Mover", "Host caching", "A snapshot of a configured disk"],
        "answer": 0,
        "explain": "The Custom Script Extension downloads and executes a script after deployment — the exam's answer whenever the stem says 'run a script automatically after the VM is deployed.' Resource Mover relocates resources between regions, host caching tunes disk I/O, and a snapshot is a disk copy, not an execution mechanism.",
    },
    {
        "type": "mc",
        "q": "A user shuts down a test VM from inside the Windows guest OS every evening, yet compute charges keep accruing overnight. Why?",
        "choices": [
            "Azure bills all VMs 24/7 regardless of power state",
            "The VM is only in the Stopped state, so the hardware stays reserved; only Stopped-deallocated releases it and stops compute billing",
            "The NSG is still processing traffic, which counts as compute",
            "The temporary disk keeps the VM in a running state",
        ],
        "answer": 1,
        "explain": "Shutting down from inside the guest OS leaves the VM merely 'Stopped' — Azure still reserves the hardware and charges for compute. Only deallocation (az vm deallocate / Stop-AzVM) releases the hardware and stops compute charges, though managed disks continue to bill. NSGs and temp disks have nothing to do with billing state.",
    },
    {
        "type": "mc",
        "q": "A compliance mandate reads: 'VM disk volumes must be encrypted inside the guest operating system with keys the company controls.' Which feature satisfies it?",
        "choices": [
            "Server-side encryption, which is already enabled",
            "Azure Disk Encryption, using BitLocker or DM-Crypt with keys in Azure Key Vault",
            "An NSG rule blocking outbound storage traffic",
            "Standard SSD disks, which include guest-level encryption",
        ],
        "answer": 1,
        "explain": "ADE encrypts the OS and data volumes inside the guest using BitLocker on Windows and DM-Crypt on Linux, with keys and secrets in an Azure Key Vault you control. SSE is storage-level and always on, so it cannot be the answer to a question about configuring encryption; disk SKU and NSGs are unrelated to volume encryption.",
    },
    {
        "type": "mc",
        "q": "An admin tries to enable Azure Disk Encryption on a VM in East US using an existing Key Vault in West Europe, and the operation fails. What is the requirement?",
        "choices": [
            "The Key Vault must use a Premium SKU",
            "The Key Vault must be in the same region and subscription as the VM",
            "The VM must be deallocated before enabling encryption",
            "ADE requires a Managed HSM, not a Key Vault",
        ],
        "answer": 1,
        "explain": "ADE requires the Key Vault holding the encryption keys to be in the same region and subscription as the VM, with the EnabledForDiskEncryption access policy set. A vault in another region is a classic wrong-answer trap; SKU is irrelevant, the VM does not need deallocation, and a standard Key Vault works fine.",
    },
    {
        "type": "mc",
        "q": "Azure Disk Encryption refuses to enable on an old Standard_A2 test VM even though the Key Vault is configured correctly. What is the most likely cause?",
        "choices": [
            "ADE is not supported on Basic/A-series or very low-memory VM sizes",
            "A-series VMs only support customer-managed SSE keys",
            "The VM is missing a public IP address",
            "ADE requires the VM to be in an availability zone",
        ],
        "answer": 0,
        "explain": "ADE is unsupported on Basic/A-series sizes and on VMs with very small memory (under about 2 GB), as well as on Ultra Disks and Premium SSD v2 — for those, use encryption at host instead. When the stem pairs a failing encryption operation with an A-series VM, the size is the reason; public IPs and zones are irrelevant to ADE.",
    },
    {
        "type": "mc",
        "q": "Security requires that data on a VM's temporary disk and in the host's disk caches also be encrypted — not just the managed disks. Which feature provides this?",
        "choices": ["Azure Disk Encryption", "Server-side encryption with platform-managed keys", "Encryption at host", "A key encryption key (KEK)"],
        "answer": 2,
        "explain": "Encryption at host encrypts data on the host server itself, including the temporary disk and host caches, and requires registering a subscription feature. ADE works inside the guest volumes and SSE covers managed disks at rest in the storage service — neither covers the temp disk and caches. A KEK merely wraps the ADE key.",
    },
    {
        "type": "mc",
        "q": "Which Azure CLI command enables Azure Disk Encryption on vm-web01 using Key Vault kv-prod-encrypt?",
        "choices": [
            "az vm encryption enable --resource-group rg-app-prod --name vm-web01 --disk-encryption-keyvault kv-prod-encrypt",
            "az disk-encryption-set create --resource-group rg-app-prod --key-url kv-prod-encrypt",
            "az vm secret add --resource-group rg-app-prod --name vm-web01 --keyvault kv-prod-encrypt",
            "az keyvault set-policy --name kv-prod-encrypt --enabled-for-disk-encryption true",
        ],
        "answer": 0,
        "explain": "az vm encryption enable (PowerShell: Set-AzVMDiskEncryptionExtension) turns on ADE for a VM, pointing at the Key Vault that will hold the keys. Disk encryption sets configure customer-managed keys for SSE — a different layer; az vm secret add stores certificates, and set-policy only prepares the vault, it does not encrypt anything.",
    },
    {
        "type": "mc",
        "q": "Finance restructures billing and a VM must move to a different subscription in the same tenant. Which PowerShell command performs the move?",
        "choices": ["Move-AzResource", "Move-AzVM", "New-AzResourceMoveCollection", "Set-AzVM -SubscriptionId"],
        "answer": 0,
        "explain": "Move-AzResource (CLI: az resource move) relocates resources across resource groups or subscriptions as a metadata operation — pass the VM together with its NIC, disks, and (cross-subscription) its virtual network. Move-AzVM does not exist, move collections belong to Azure Resource Mover for region moves, and Set-AzVM cannot change subscription.",
    },
    {
        "type": "mc",
        "q": "During a resource move started with az resource move, a teammate tries to deploy a new VM into the source resource group and the deployment is blocked. Why?",
        "choices": [
            "The subscription hits its vCPU quota during any move",
            "Both the source and destination resource groups are locked for create, update, and delete operations until the move completes",
            "Azure Policy automatically denies deployments during moves",
            "The source resource group is deleted as soon as the move starts",
        ],
        "answer": 1,
        "explain": "While a move is in progress, Azure locks both the source and destination resource groups: no create, update, or delete operations succeed until it finishes, though reads still work. This is a favorite 'what happens during the move?' question. Quota, policy, and deletion are not involved.",
    },
    {
        "type": "mc",
        "q": "An admin moves a VM from a resource group located in East US to a resource group whose location is West US. Where does the VM physically run afterward?",
        "choices": [
            "West US, because the VM inherits the resource group's location",
            "East US — a resource group move is metadata only and never changes the VM's region",
            "Both regions, in an active-active configuration",
            "The move fails, because regions must match",
        ],
        "answer": 1,
        "explain": "A resource group's location is metadata about where the group's management data lives; moving a VM between resource groups never relocates the VM itself. To actually change the VM's region you must replicate it with Azure Resource Mover. The move succeeds — the region mismatch is not an error, just a trap.",
    },
    {
        "type": "mc",
        "q": "A data-residency requirement forces a production VM to relocate from North Europe to Germany West Central. Which tool is designed for this?",
        "choices": ["az resource move", "Move-AzResource", "Azure Resource Mover (built on Azure Site Recovery)", "az vm redeploy"],
        "answer": 2,
        "explain": "Cross-region moves require physically replicating the data, so Azure Resource Mover — built on Site Recovery — validates dependencies, replicates, lets you test, then commits the move with new resource IDs and IPs. Move-AzResource / az resource move only change resource group or subscription metadata, and az vm redeploy just moves the VM to a new host in the same region.",
    },
    {
        "type": "mc",
        "q": "A cross-subscription move validation fails for one VM in the batch. The failing VM's disks are encrypted with Azure Disk Encryption. What must the admin do first?",
        "choices": [
            "Rotate the key encryption key in Key Vault",
            "Disable Azure Disk Encryption on the VM, because ADE-encrypted VMs cannot move across subscriptions",
            "Convert the disks from Premium SSD to Standard SSD",
            "Delete the VM's NSG before retrying the move",
        ],
        "answer": 1,
        "explain": "VMs with ADE-encrypted disks cannot move across subscriptions — the encryption must be disabled first, then re-enabled after the move. Cross-subscription moves also require both subscriptions to share the same Microsoft Entra tenant. Key rotation, disk SKU, and NSGs do not affect move validation.",
    },
    {
        "type": "mc",
        "q": "An admin runs az vm resize but the required Standard_E8s_v5 size does not appear in the list of available sizes for the VM. What should be done first?",
        "choices": [
            "Deallocate the VM so it can restart on any cluster in the region that offers the size",
            "Open a support ticket to enable the size on the subscription",
            "Move the VM to a new resource group in the same region",
            "Delete and recreate the virtual network",
        ],
        "answer": 0,
        "explain": "az vm list-vm-resize-options / Get-AzVMSize only shows sizes available on the VM's current hardware cluster. Deallocating frees the VM from that cluster, so on restart it can land on any cluster in the region offering the target size (availability-set members must all be deallocated together). Resource group and VNet changes have no effect on size availability.",
    },
    {
        "type": "mc",
        "q": "A single production database VM must carry a financially backed 99.9% SLA without adding a second VM. What is required?",
        "choices": [
            "Premium SSD (or better) on every disk attached to the VM",
            "An availability set with one VM in it",
            "A Standard SSD OS disk with ReadWrite host caching",
            "Deployment into any availability zone",
        ],
        "answer": 0,
        "explain": "The single-instance VM SLA of 99.9% requires Premium SSD or better on all of the VM's disks, and needs an s-capable size such as D4s. An availability set needs 2+ VMs before its 99.95% SLA applies, host caching is a performance setting, and a single zonal VM alone does not reach 99.99% — that figure requires 2+ VMs across zones.",
    },
    {
        "type": "mc",
        "q": "An admin tries to create a new VM whose OS disk is an Ultra Disk to maximize boot performance, and the deployment fails. Why?",
        "choices": [
            "Ultra Disks require a separate storage account",
            "Ultra Disks can only be attached as data disks, never used as OS disks",
            "Ultra Disks are HDD-backed and too slow to boot from",
            "Ultra Disks only work inside availability sets",
        ],
        "answer": 1,
        "explain": "Ultra Disk and Premium SSD v2 are data-disk-only — the OS disk must be one of Premium SSD, Standard SSD, or Standard HDD. Ultra Disks are the top SSD tier with IOPS and throughput tunable independently of size, they are managed disks needing no storage account, and they have no availability-set dependency.",
    },
    {
        "type": "mc",
        "q": "After a cost-saving script deallocates VMs nightly, a user reports that scratch files saved on the D: drive of a Windows VM vanish every morning. What happened?",
        "choices": [
            "The nightly script deletes the data disks",
            "D: is the temporary disk — local host storage whose contents are lost on deallocate, redeploy, or host maintenance",
            "Azure Backup restored an older version of the drive",
            "SSE encrypted the files and made them unreadable",
        ],
        "answer": 1,
        "explain": "The temporary disk (D: on Windows, /dev/sdb on Linux) is local storage on the host meant for page/swap files, and it is wiped whenever the VM is deallocated, redeployed, or moved by host maintenance. Data that must persist belongs on managed data disks. No deletion, backup restore, or encryption is involved.",
    },
    {
        "type": "mc",
        "q": "A 512-GiB data disk is mostly empty, and an admin wants to reduce it to 256 GiB to cut costs. What is the correct approach?",
        "choices": [
            "Run az disk update --size-gb 256 while the VM is deallocated",
            "Create a snapshot and restore it at 256 GiB",
            "Managed disks can be expanded but never shrunk — create a new smaller disk and migrate the data",
            "Change the disk from Premium SSD to Standard HDD, which halves its size",
        ],
        "answer": 2,
        "explain": "Managed disks only grow; there is no shrink operation, so az disk update with a smaller size fails and snapshots restore at the original size or larger. The only path is creating a new smaller disk, copying the data over, and swapping it in. Changing the performance tier changes price per GiB, not capacity.",
    },
    {
        "type": "mc",
        "q": "Two web VMs must keep serving traffic even if an entire Azure datacenter in the region is lost, and the design must earn a 99.99% SLA. How should they be deployed?",
        "choices": [
            "Both VMs in one availability set with 3 fault domains",
            "The two VMs in different availability zones behind a Standard load balancer",
            "One VM with Premium SSD disks and daily snapshots",
            "Both VMs on the same host with ReadOnly caching",
        ],
        "answer": 1,
        "explain": "Availability zones are physically separate datacenters with independent power, cooling, and networking; 2+ VMs spread across zones earn the 99.99% SLA, fronted by a zone-redundant Standard load balancer. An availability set only protects against rack/switch failures inside one datacenter (99.95%), and a single VM tops out at 99.9%.",
    },
    {
        "type": "mc",
        "q": "A scale set is configured with minimum 2, maximum 10, default 3, and CPU-based rules, but during a traffic spike it stops adding instances at 10 even though CPU stays above the threshold. Why?",
        "choices": [
            "Azure caps every scale set at 10 instances",
            "Autoscale never exceeds the profile's configured maximum — the cap of 10 is the admin's own setting",
            "The load balancer ran out of backend pool slots",
            "The cooldown period permanently disabled scale-out",
        ],
        "answer": 1,
        "explain": "Autoscale always honors the profile's minimum and maximum bounds; if a set 'won't scale past 10,' the maximum is set to 10 — not a platform limit, since VMSS supports up to 1,000 instances with platform images. Cooldown only spaces out consecutive actions, and backend pools are not the constraint.",
    },
    {
        "type": "mc",
        "q": "A quorum-based database cluster needs scale-set management but must mix two different VM sizes in the same set. Which orchestration mode supports this?",
        "choices": [
            "Uniform orchestration",
            "Flexible orchestration, where instances are full standard VMs that can mix sizes and types",
            "Rolling orchestration",
            "Automatic orchestration",
        ],
        "answer": 1,
        "explain": "Flexible orchestration manages full standard VMs, allowing mixed instance sizes/types and better fault-domain flexibility — it is the recommended default for new deployments and suits quorum-based or stateful workloads. Uniform mode keeps identical instances defined by a single scale-set model; Rolling and Automatic are upgrade policies, not orchestration modes.",
    },
    {
        "type": "mc",
        "q": "A new image must be pushed to all 40 instances of a production scale set without taking the whole service down. Which upgrade policy should be configured?",
        "choices": [
            "Automatic, which updates all instances at once",
            "Manual, which never applies the new image",
            "Rolling, which upgrades instances in batches with health checks between them",
            "Flexible, which staggers upgrades by fault domain",
        ],
        "answer": 2,
        "explain": "The Rolling upgrade policy applies a new model or image in batches, verifying health between batches so the fleet keeps serving traffic. Automatic upgrades everything at once and can cause a brief full outage; Manual leaves each instance for the admin to upgrade (it does eventually apply, but by hand); Flexible is an orchestration mode, not an upgrade policy.",
    },
    # --- true/false ---
    {
        "type": "tf",
        "q": "True or False: Deleting an Azure VM does not, by default, delete its NIC, managed disks, or public IP address.",
        "answer": True,
        "explain": "These are separate resources that survive VM deletion by default and keep billing — a classic source of orphaned resources. The portal offers checkboxes (and the CLI has flags) to delete them with the VM, but you must opt in.",
    },
    {
        "type": "tf",
        "q": "True or False: Resizing a VM with az vm resize is non-disruptive and requires no restart when the new size is available on the current cluster.",
        "answer": False,
        "explain": "Resizing always restarts the VM, so it needs a maintenance window — and if the target size requires a different hardware cluster, the VM must be deallocated first, which also releases dynamic public IPs and wipes the temporary disk. There is no zero-downtime resize.",
    },
    {
        "type": "tf",
        "q": "True or False: An existing running VM can be added to an availability set without recreating it.",
        "answer": False,
        "explain": "A VM joins an availability set only at creation time. To bring an existing VM into a set you must delete the VM (keeping its disks) and recreate it in the set. The same recreate rule applies to combining constructs: a VM cannot be in both an availability set and an availability zone.",
    },
    {
        "type": "tf",
        "q": "True or False: A scale set configured with only a scale-out rule will still automatically remove instances when the load drops.",
        "answer": False,
        "explain": "Scale-in never happens on its own — every scale-out rule needs a matching scale-in rule, or the set grows during spikes and never shrinks, burning money indefinitely. Autoscale only performs the actions its rules define, within the profile's min/max bounds.",
    },
    {
        "type": "tf",
        "q": "True or False: Moving a VM to a subscription in a different Microsoft Entra tenant is supported with Move-AzResource.",
        "answer": False,
        "explain": "Cross-subscription moves require both subscriptions to be in the same Microsoft Entra tenant; to change tenants you must transfer the subscription itself, not move resources. Remember the other cross-subscription blocker too: ADE-encrypted VMs must be decrypted before the move.",
    },
    # --- fill in ---
    {
        "type": "fill",
        "q": "To stop compute charges for a VM overnight while keeping its managed disks, an admin runs az vm ______. (type the subcommand)",
        "answers": ["deallocate", "de-allocate", "stop --deallocate", "deallocated"],
        "explain": "az vm deallocate (PowerShell: Stop-AzVM) puts the VM into the Stopped-deallocated state: the hardware is released and compute billing stops, though disks remain and continue to bill. A guest-OS shutdown only reaches 'Stopped,' which still incurs compute charges, and deallocation releases dynamic IPs and wipes the temp disk.",
    },
    {
        "type": "fill",
        "q": "The VM extension that downloads and runs a script automatically after deployment — the standard way to install software on a new VM — is the ______ Extension. (type your answer)",
        "answers": ["custom script", "custom script extension", "customscript", "cse"],
        "explain": "The Custom Script Extension executes a script post-deployment, e.g. installing a web server or joining a config-management system. Desired State Configuration handles ongoing configuration and Run Command covers one-off commands, but 'run a script after deploy' always means the Custom Script Extension.",
    },
    {
        "type": "fill",
        "q": "On Windows VMs, Azure Disk Encryption encrypts volumes inside the guest using ______. (type the technology name)",
        "answers": ["bitlocker", "bit locker"],
        "explain": "ADE uses BitLocker on Windows and DM-Crypt on Linux to encrypt OS and data volumes inside the guest, storing keys and secrets in an Azure Key Vault in the same region and subscription as the VM. This guest-level layer is distinct from always-on SSE and from encryption at host.",
    },
    {
        "type": "fill",
        "q": "The Azure service, built on Site Recovery, that validates dependencies, replicates, tests, and commits a VM's move to a different region is Azure ______. (type your answer)",
        "answers": ["resource mover", "azure resource mover", "resourcemover"],
        "explain": "Azure Resource Mover handles cross-region moves, which are replication operations because the data must physically relocate — expect new resource IDs, new IPs, and usually a new virtual network at the destination. Resource-group and subscription moves are metadata-only and use Move-AzResource / az resource move instead.",
    },
    {
        "type": "fill",
        "q": "The waiting period after an autoscale action, during which no further scale actions fire so the last one can take effect, is called the ______ period. (type your answer)",
        "answers": ["cooldown", "cool down", "cool-down"],
        "explain": "Each autoscale rule includes a cooldown so a scale action can settle and its metric impact be measured before the next action triggers, preventing flapping. It spaces actions out — it never overrides the profile's minimum/maximum bounds or a missing scale-in rule.",
    },
    # --- matching ---
    {
        "type": "match",
        "q": "Match each encryption layer or component to its description.",
        "pairs": [
            ["Server-side encryption (SSE)", "Always-on encryption of managed disks at rest in the storage service; cannot be disabled"],
            ["Azure Disk Encryption (ADE)", "BitLocker/DM-Crypt volume encryption inside the guest OS, keys in Key Vault"],
            ["Encryption at host", "Encrypts data on the host server, including the temporary disk and caches"],
            ["Key encryption key (KEK)", "Optional Key Vault key that wraps the ADE encryption key"],
        ],
        "explain": "Match the phrase to the layer: 'always on, no action needed' is SSE; 'BitLocker/DM-Crypt with keys in Key Vault' is ADE; 'temp disk and host caches too' is encryption at host; and a KEK is an extra wrapping key, not an encryption layer itself. The exam loves making you tell these apart.",
    },
    {
        "type": "match",
        "q": "Match each VM size family letter to the workload it is optimized for.",
        "pairs": [
            ["B-series", "Burstable — low baseline CPU with occasional spikes at lowest cost"],
            ["D-series", "General purpose — balanced CPU-to-memory for web servers and small databases"],
            ["E-series", "Memory optimized — in-memory caches, SAP, large relational databases"],
            ["F-series", "Compute optimized — batch processing and analytics needing high CPU"],
            ["L-series", "Storage optimized — high local disk throughput for big data and NoSQL"],
            ["N-series", "GPU — machine-learning training, rendering, video processing"],
        ],
        "explain": "The exam hands you a workload adjective and expects the letter back: B burstable, D general, E memory, F compute, L storage, N GPU. Anchor them with mnemonics — Burstable, Default, Extra memory, Fast CPU, Local disks, Nvidia.",
    },
    {
        "type": "match",
        "q": "Match each resilience construct to what it protects against or guarantees.",
        "pairs": [
            ["Fault domain", "Unplanned failure of a shared rack, power source, or network switch"],
            ["Update domain", "Planned maintenance — Azure reboots only one at a time"],
            ["Availability set (2+ VMs)", "99.95% SLA within a single datacenter"],
            ["Availability zones (2+ VMs)", "99.99% SLA across physically separate datacenters"],
            ["Single VM, all Premium SSD disks", "99.9% SLA for one instance"],
        ],
        "explain": "Map the threat to the construct: rack/power/switch failures are fault domains, planned maintenance is update domains, datacenter loss requires availability zones. Then quote the SLA ladder cold: 99.9 (single VM on Premium SSD) / 99.95 (availability set) / 99.99 (zones).",
    },
    # --- ordering ---
    {
        "type": "order",
        "q": "Arrange these VM deployments from LOWEST to HIGHEST availability SLA.",
        "items": [
            "Single VM with Standard HDD disks",
            "Single VM with Standard SSD disks",
            "Single VM with Premium SSD on all disks (99.9%)",
            "Two or more VMs in an availability set (99.95%)",
            "Two or more VMs across availability zones (99.99%)",
        ],
        "explain": "Single-instance SLAs climb with disk tier — Standard HDD, then Standard SSD, then Premium SSD or better on all disks at 99.9%. Adding a second VM in an availability set raises the compute SLA to 99.95%, and spreading 2+ VMs across availability zones tops the ladder at 99.99%.",
    },
    {
        "type": "order",
        "q": "The size a VM needs is not shown in the resize list. Arrange the admin's steps in the correct order.",
        "items": [
            "Confirm the size is missing with az vm list-vm-resize-options",
            "Deallocate the VM with az vm deallocate",
            "Resize with az vm resize --size Standard_E8s_v5",
            "Start the VM with az vm start",
            "Verify the app, noting any dynamic public IP change and the wiped temporary disk",
        ],
        "explain": "The resize list only shows sizes on the current hardware cluster, so a missing size means deallocate first — a deallocated VM can restart on any cluster in the region offering the size. After resizing and starting, remember the side effects: deallocation releases dynamic IPs and erases the temp disk, and every resize reboots the VM.",
    },
    {
        "type": "order",
        "q": "Arrange the steps of moving a VM to another region with Azure Resource Mover, from first to last.",
        "items": [
            "Add the VM and its dependent resources to a move collection",
            "Resolve dependencies so nothing required is left behind",
            "Prepare and replicate the resources to the target region via Site Recovery",
            "Initiate the move and test the VM in the target region",
            "Commit the move and clean up the source resources",
        ],
        "explain": "Region moves are replication operations: Resource Mover validates dependencies, replicates the data through Site Recovery, lets you test the result, and only then commits — leaving new resource IDs, new IPs, and usually a new VNet in the target region. Compare with resource-group/subscription moves, which are instant metadata operations via Move-AzResource.",
    },
]
