TOPIC = {
    "id": "3.1",
    "domain": 3,
    "title": "Automate deployment of resources by using ARM templates or Bicep files.",
    "short": "ARM Templates & Bicep",
}

TERMS = [
    ("ARM template", "JSON file that declaratively describes the end state of Azure resources; Azure Resource Manager figures out how to get there"),
    ("Bicep", "Domain-specific language that transpiles to ARM JSON before deployment — same engine, same capabilities, much less syntax"),
    ("Declarative", "Style of infrastructure-as-code where you state the desired end state rather than the steps to reach it"),
    ("Idempotent", "Property of templates that lets you deploy the same file repeatedly and get the same result — no duplicates, no errors for existing resources"),
    ("resources section", "The only REQUIRED content section of an ARM template — the resources to deploy or update (the $schema and contentVersion elements are also mandatory)"),
    ("parameters section", "ARM template section holding values supplied at deployment time (environment name, SKU, credentials)"),
    ("variables section", "ARM template section holding values computed inside the template to avoid repeating expressions"),
    ("outputs section", "ARM template section returning values after deployment, such as connection strings and resource IDs"),
    ("apiVersion", "Resource field that pins which version of the provider's REST API the properties are validated against"),
    ("Template expression", "Anything inside \"[...]\" in an ARM template — evaluated at deployment time using template functions"),
    ("uniqueString()", "Template function producing a deterministic 13-character hash — the standard trick for globally unique storage account names"),
    ("resourceGroup().location", "Template function returning the deployment resource group's region — the house pattern for a location default"),
    ("dependsOn", "Explicit ordering array in ARM JSON; only accepts resources defined in the same template"),
    ("resourceId()", "Template function that builds a resource's full ID from its type and name, typically used inside dependsOn"),
    ("secureString", "ARM parameter type for passwords and secrets — never plain string, never given a defaultValue"),
    ("allowedValues", "ARM parameter property that restricts a parameter to a specific list of permitted values"),
    ("@secure()", "Bicep decorator marking a parameter as secret so its value is excluded from deployment history and logs"),
    ("@allowed", "Bicep decorator restricting a parameter to a list of permitted values — the JSON allowedValues equivalent"),
    ("module (Bicep)", "Bicep keyword that deploys another .bicep file as a nested deployment — the clean replacement for ARM's linked templates"),
    ("existing keyword", "Bicep keyword pointing a symbolic name at a resource already deployed, so you can read its properties without redeploying it"),
    ("Symbolic name", "Identifier for a Bicep resource; referencing it from another resource creates an implicit dependency, making dependsOn rarely needed"),
    ("String interpolation", "Bicep's '${prefix}${suffix}' syntax for building strings — the idiomatic replacement for concat()"),
    ("Incremental mode", "Default deployment mode: resources in the template are created/updated, resources NOT in the template are left unchanged"),
    ("Complete mode", "Deployment mode that DELETES resources not defined in the template; valid at resource-group scope only"),
    ("What-if", "Deployment preview (--what-if / -WhatIf) showing Create/Modify/Delete/NoChange per resource without deploying anything"),
    (".bicepparam file", "Bicep-native parameter file that starts with using 'main.bicep' and assigns param name = value pairs"),
    ("Template spec", "Template stored as a versioned Azure resource, shared via RBAC instead of a storage account SAS URL"),
    ("az bicep decompile", "CLI command converting ARM JSON to Bicep — a best-effort translation that needs review"),
    ("az bicep build", "CLI command transpiling a Bicep file into ARM JSON — the same deterministic output deployment commands generate on the fly"),
    ("Export template", "Resource group action (az group export) generating a best-effort template of the CURRENT state, including portal-made drift"),
    ("Deployment history", "The Deployments blade record holding each deployment's inputs, outputs, status, and the EXACT template submitted"),
]

QUESTIONS = [
    # --- multiple choice ---
    {
        "type": "mc",
        "q": "An admin strips a working ARM template down to the bare minimum for a quick test. Which top-level section is the ONLY one the template must still contain?",
        "choices": ["parameters", "resources", "outputs", "variables"],
        "answer": 1,
        "explain": "resources is the only required section of an ARM template — it lists what to deploy or update. parameters, variables, functions, and outputs are all optional conveniences, though the $schema and contentVersion top-level elements must also stay alongside resources. This 'which section is required' fact is a staple exam question.",
    },
    {
        "type": "mc",
        "q": "A template names a storage account with \"[concat(parameters('prefix'), uniqueString(resourceGroup().id))]\". What does the uniqueString() portion produce?",
        "choices": [
            "A random GUID that changes on every deployment",
            "A deterministic 13-character hash derived from the resource group ID",
            "A timestamp appended at deployment time",
            "A sequential counter incremented per resource",
        ],
        "answer": 1,
        "explain": "uniqueString() generates a deterministic 13-character hash of its input — the same resource group ID always yields the same string, so redeployments stay idempotent while the name is globally unique-enough for a storage account. A random GUID or timestamp would change on every run and break idempotency; there is no sequential counter function.",
    },
    {
        "type": "mc",
        "q": "A template sets every resource's location to \"[resourceGroup().location]\". A colleague deploys it into a resource group in West Europe. Where do the resources land?",
        "choices": [
            "The subscription's default region",
            "East US, the template author's original region",
            "West Europe, the deployment resource group's region",
            "Deployment fails because no explicit region was given",
        ],
        "answer": 2,
        "explain": "resourceGroup().location evaluates at deployment time to the region of the resource group you deploy into — here, West Europe. That is exactly why it is the house pattern for a location default: the template follows the resource group instead of hard-coding a region. Subscriptions have no default region, and the expression makes an explicit value unnecessary.",
    },
    {
        "type": "mc",
        "q": "After a deployment, an admin finds a storage account literally named parameters('storageName') in the portal. What went wrong in the template?",
        "choices": [
            "The parameter was declared without a defaultValue",
            "The expression was written without the enclosing square brackets, so it was treated as a literal string",
            "The parameters section was placed after the resources section",
            "The parameter type was string instead of secureString",
        ],
        "answer": 1,
        "explain": "ARM template expressions must sit inside \"[...]\" — \"parameters('storageName')\" without the brackets is just a literal string, which Resource Manager happily used as the name. Section order in the JSON doesn't matter, a missing defaultValue would cause a prompt or an error rather than a literal name, and secureString relates to secrets, not evaluation.",
    },
    {
        "type": "mc",
        "q": "A teammate argues the company should avoid Bicep because 'some resources can only be deployed with real ARM templates.' Which statement is accurate?",
        "choices": [
            "Bicep transpiles to ARM JSON and uses the same Resource Manager API, so anything a template can deploy, Bicep can too",
            "Bicep uses its own deployment engine that supports fewer resource types",
            "Bicep is imperative, so it cannot guarantee the same end state as a template",
            "Bicep files must be manually converted to JSON before any deployment command accepts them",
        ],
        "answer": 0,
        "explain": "Bicep is not a different engine — it compiles to the same ARM JSON and hits the same Resource Manager API, so there is nothing a template can deploy that Bicep cannot. It is just as declarative and idempotent as JSON, and both az deployment group create and New-AzResourceGroupDeployment accept .bicep files directly, transpiling automatically.",
    },
    {
        "type": "mc",
        "q": "In a resource declaration, what is the purpose of the apiVersion field, such as \"apiVersion\": \"2023-05-01\"?",
        "choices": [
            "It records the template author's own version number for the file",
            "It pins which version of the resource provider's REST API the properties are validated against",
            "It states the version of the JSON schema defining the template language",
            "It selects which Azure CLI version must run the deployment",
        ],
        "answer": 1,
        "explain": "apiVersion picks the version of that provider's REST API, which determines which properties are valid for the resource. The template author's own version string is contentVersion, and the template-language schema is $schema — the exam loves swapping these three. The CLI version is unrelated to any field in the template.",
    },
    {
        "type": "mc",
        "q": "An admin must edit an ARM template so the storageSku parameter accepts only Standard_LRS, Standard_GRS, or Standard_ZRS. Which parameter property enforces this?",
        "choices": ["allowedValues", "defaultValue", "minLength", "constraints"],
        "answer": 0,
        "explain": "allowedValues lists the exact permitted values, and deployments supplying anything else fail validation. defaultValue only makes the parameter optional, minLength/maxLength constrain string length rather than content, and 'constraints' is not an ARM parameter property. In Bicep the same job is done by the @allowed decorator.",
    },
    {
        "type": "mc",
        "q": "In an ARM JSON template, a virtual machine intermittently fails to deploy because its NIC does not exist yet. What is the correct fix?",
        "choices": [
            "Add a dependsOn array on the VM containing \"[resourceId('Microsoft.Network/networkInterfaces', variables('nicName'))]\"",
            "Move the NIC earlier in the resources array so it deploys first",
            "Add a dependsOn array on the NIC pointing at the VM",
            "Reference the NIC by its symbolic name so the dependency is implicit",
        ],
        "answer": 0,
        "explain": "ARM JSON has no symbolic names, so ordering between resources in the same template needs an explicit dependsOn, typically built with resourceId(). Array order does not control deployment order — Resource Manager deploys in parallel unless told otherwise. The dependency belongs on the VM (the dependent resource), and symbolic-name implicit dependencies are a Bicep feature, not JSON.",
    },
    {
        "type": "mc",
        "q": "A reviewer flags an ARM template where adminPassword is declared as type string with a defaultValue of 'P@ssw0rd1'. What is the correct remediation?",
        "choices": [
            "Change the type to secureString, remove the default, and supply the value from Azure Key Vault at deployment time",
            "Keep the type but move the default into the variables section",
            "Change the type to secureObject and keep the default for convenience",
            "Base64-encode the default value so it is not readable in the file",
        ],
        "answer": 0,
        "explain": "Passwords belong in secureString parameters with no defaultValue, ideally referenced from Key Vault in the parameter file — secure parameters are excluded from deployment history and logs. Moving the secret to variables or encoding it still leaves it in the template, and secureObject is for structured secrets, and giving it a default repeats the original mistake.",
    },
    {
        "type": "mc",
        "q": "A template hard-codes \"location\": \"eastus\" on every resource, and deployments to other regions keep landing in East US. What is the recommended one-line pattern to fix it?",
        "choices": [
            "Add a location parameter whose defaultValue is \"[resourceGroup().location]\" and reference it on each resource",
            "Replace eastus with \"[subscription().location]\" on each resource",
            "Add a location entry to the variables section set to the string 'eastus'",
            "Delete the location field so Azure picks the nearest region automatically",
        ],
        "answer": 0,
        "explain": "The house pattern is a location parameter defaulting to resourceGroup().location — resources follow whatever resource group you deploy into, and callers can still override it. Subscriptions do not have a location function that works this way, a variable set to 'eastus' just moves the hard-coding, and omitting location fails validation for most resource types.",
    },
    {
        "type": "mc",
        "q": "In a Bicep file, an admin must restrict the vmSize parameter to three approved sizes. Which syntax is correct?",
        "choices": [
            "@allowed([ 'Standard_B2s', 'Standard_D2s_v5', 'Standard_D4s_v5' ]) placed above the param declaration",
            "@allowedValues([ ... ]) placed above the param declaration",
            "An allowedValues property inside the param declaration's braces",
            "@constraint('vmSize in list') placed above the param declaration",
        ],
        "answer": 0,
        "explain": "Bicep uses decorators above the declaration, and the value-restriction decorator is @allowed — not @allowedValues, which mixes up the JSON property name with the decorator. Bicep params do not take an allowedValues property in braces, and @constraint does not exist. Other real decorators include @minLength, @maxLength, @description, and @secure().",
    },
    {
        "type": "mc",
        "q": "A Bicep parameter sqlAdminPassword must never appear in deployment history or logs. Which change achieves this?",
        "choices": [
            "Add the @secure() decorator above the param declaration",
            "Rename the parameter with a leading underscore",
            "Declare it as var instead of param",
            "Add the @description('secret') decorator above the param declaration",
        ],
        "answer": 0,
        "explain": "@secure() is Bicep's equivalent of the JSON secureString/secureObject types: the value is excluded from deployment history and logs, and it should not carry a meaningful default. Naming conventions have no security effect, a var is computed in the file (and visible), and @description is documentation only.",
    },
    {
        "type": "mc",
        "q": "A new Bicep file must read properties of a Key Vault named corp-shared-kv that was deployed last year in the same resource group — without redeploying or modifying it. What should the admin write?",
        "choices": [
            "resource kv 'Microsoft.KeyVault/vaults@2023-07-01' existing = { name: 'corp-shared-kv' }",
            "module kv 'corp-shared-kv.bicep' = { name: 'kvDeploy' }",
            "resource kv 'Microsoft.KeyVault/vaults@2023-07-01' = { name: 'corp-shared-kv' }",
            "import kv from 'Microsoft.KeyVault/vaults/corp-shared-kv'",
        ],
        "answer": 0,
        "explain": "The existing keyword points a symbolic name at a resource that is already deployed, letting you read its properties without including it in the deployment. Declaring it without existing would attempt to deploy/update the vault, module deploys another Bicep file as a nested deployment, and import is not how Bicep references deployed resources.",
    },
    {
        "type": "mc",
        "q": "The Bicep linter flags the line var vmName = concat(prefix, '-vm-', env). What is the idiomatic fix?",
        "choices": [
            "Rewrite it with string interpolation: var vmName = '${prefix}-vm-${env}'",
            "Wrap the expression in square brackets: var vmName = '[concat(prefix)]'",
            "Move the concat() call into the template's functions section",
            "Replace concat() with the union() function",
        ],
        "answer": 0,
        "explain": "Bicep's idiomatic string building is interpolation with '${...}' — the linter actively flags concat() in Bicep files. Square-bracket expressions are ARM JSON syntax, not Bicep; the functions section is an ARM JSON concept for user-defined functions; and union() merges objects and arrays, not strings.",
    },
    {
        "type": "mc",
        "q": "A code review finds a Bicep NIC resource that sets subnet ID to vnet.properties.subnets[0].id AND also lists the VNet in a dependsOn block. What is the recommended improvement?",
        "choices": [
            "Remove the dependsOn block — the symbolic reference already creates an implicit dependency",
            "Remove the symbolic reference and keep only the dependsOn block",
            "Keep both so the dependency is guaranteed twice",
            "Replace dependsOn with a reference() function call",
        ],
        "answer": 0,
        "explain": "In Bicep, referencing another resource's symbolic name creates an implicit dependency, so Resource Manager already deploys the VNet first — a dependsOn that merely restates the reference is redundant and should be removed. Dropping the reference would lose the actual property value, duplication adds noise without benefit, and reference() is an ARM JSON function, not the Bicep idiom.",
    },
    {
        "type": "mc",
        "q": "An admin needs to deploy main.bicep into the existing resource group RG1 using the Azure CLI. Which command is correct?",
        "choices": [
            "az deployment group create --resource-group RG1 --template-file main.bicep",
            "az deployment sub create --resource-group RG1 --template-file main.bicep",
            "az group deployment export --resource-group RG1 --template-file main.bicep",
            "az bicep build --resource-group RG1 --file main.bicep",
        ],
        "answer": 0,
        "explain": "Resource-group-scope deployments use az deployment group create, which accepts .bicep files directly — the CLI transpiles them to ARM JSON automatically. az deployment sub create targets subscription scope and takes --location, not --resource-group; export retrieves templates rather than deploying; and az bicep build only converts Bicep to JSON without deploying anything.",
    },
    {
        "type": "mc",
        "q": "A template's job is to CREATE three new resource groups and place policy assignments in them. Which command can run this deployment?",
        "choices": [
            "az deployment sub create --location eastus --template-file main.bicep",
            "az deployment group create --resource-group RG1 --template-file main.bicep",
            "az group create --template-file main.bicep",
            "az ts create --name rgSpec --template-file main.bicep",
        ],
        "answer": 0,
        "explain": "Resource groups can only be created by a deployment at a scope above them — subscription scope — so az deployment sub create is required, and it needs --location for the deployment metadata since there is no resource group to inherit from. A group-scope deployment cannot create its own container, az group create makes one RG without a template, and az ts create stores a template spec rather than deploying.",
    },
    {
        "type": "mc",
        "q": "A PowerShell-based pipeline must deploy azuredeploy.json with its parameter file into resource group RG1. Which cmdlet does this?",
        "choices": [
            "New-AzResourceGroupDeployment -ResourceGroupName RG1 -TemplateFile ./azuredeploy.json -TemplateParameterFile ./azuredeploy.parameters.json",
            "New-AzResourceGroup -Name RG1 -TemplateFile ./azuredeploy.json",
            "New-AzSubscriptionDeployment -ResourceGroupName RG1 -TemplateFile ./azuredeploy.json",
            "Export-AzResourceGroup -ResourceGroupName RG1 -TemplateFile ./azuredeploy.json",
        ],
        "answer": 0,
        "explain": "New-AzResourceGroupDeployment is the PowerShell counterpart of az deployment group create for resource-group-scope deployments. New-AzResourceGroup only creates an empty resource group, New-AzSubscriptionDeployment is for subscription scope (creating RGs, policies) and takes -Location rather than a resource group, and Export-AzResourceGroup pulls a template out instead of pushing one in.",
    },
    {
        "type": "mc",
        "q": "After a scheduled pipeline run, several manually created VMs vanished from a shared resource group. The pipeline deploys a partial template nightly. What most likely happened?",
        "choices": [
            "The deployment ran in Complete mode, which deletes resources not defined in the template",
            "The deployment ran in Incremental mode, which removes unmanaged resources",
            "The what-if operation executed the deletions it previewed",
            "The template's contentVersion changed, forcing Azure to recreate the group",
        ],
        "answer": 0,
        "explain": "Complete mode enforces the template as the whole truth: anything in the resource group that the template does not mention is deleted — the classic 'why did my VM disappear' trap, which is why it demands extreme care in shared groups. Incremental (the default) leaves unlisted resources untouched, what-if only previews and never changes anything, and Azure does not interpret contentVersion at all.",
    },
    {
        "type": "mc",
        "q": "Before running a refactored template against production, an admin wants a per-resource preview of exactly what would be created, modified, or deleted — with no changes made. Which option provides this?",
        "choices": [
            "Add --what-if to az deployment group create",
            "Add --mode Validate to az deployment group create",
            "Run az deployment group export before deploying",
            "Deploy to production with --mode Incremental and roll back if needed",
        ],
        "answer": 0,
        "explain": "The what-if operation (--what-if in CLI, -WhatIf in PowerShell) diffs the template against reality and reports Create/Modify/Delete/NoChange per resource without deploying; --confirm-with-what-if shows the preview and then asks before proceeding. There is no Validate deployment mode flag in this form, export retrieves a past deployment's template, and deploying-then-rolling-back is exactly what what-if exists to avoid.",
    },
    {
        "type": "mc",
        "q": "An organization wants its approved VM template stored as a versioned Azure resource that teams deploy by ID, with access controlled by RBAC instead of a storage account SAS URL. What should be created?",
        "choices": [
            "A template spec via az ts create",
            "A managed image in a Shared Image Gallery",
            "A blueprint assignment",
            "A GitHub release containing azuredeploy.json",
        ],
        "answer": 0,
        "explain": "Template specs (az ts create / New-AzTemplateSpec) store an ARM or Bicep template as a versioned Azure resource, shared through RBAC and deployable with --template-spec — solving the template-sharing problem without SAS-protected storage. Image galleries version VM images rather than templates, Blueprints is a different (deprecated-path) governance service, and a GitHub release provides neither Azure-native versioning nor RBAC-scoped deployment by ID.",
    },
    {
        "type": "mc",
        "q": "A resource group was built by hand in the portal over two years. The admin needs a template capturing its CURRENT state, including all that manual drift, as a starting point for automation. What should they use?",
        "choices": [
            "The resource group's Export template feature (az group export)",
            "The most recent entry in the Deployments blade",
            "az bicep build against the resource group",
            "The activity log filtered to write operations",
        ],
        "answer": 0,
        "explain": "Export template generates a template from the resource group's current state, portal-made changes included — with the caveat that it is best-effort, may miss some resource types or settings, and needs cleanup. Deployment history only shows what past deployments submitted, not later drift; az bicep build converts a local Bicep file, not live resources; and the activity log records operations, not a deployable template.",
    },
    {
        "type": "mc",
        "q": "An auditor asks for the EXACT template and parameters submitted for last month's deployment named storageDeploy in RG1 — not the resource group's current state. Where does the admin get it?",
        "choices": [
            "The deployment history: select the deployment in the Deployments blade, or run az deployment group export -g RG1 -n storageDeploy",
            "The resource group's Export template feature",
            "az bicep decompile against the resource group",
            "The resource group's tags and properties blade",
        ],
        "answer": 0,
        "explain": "Every deployment is recorded in the resource group's Deployments blade with its inputs, outputs, status, and the exact template submitted — retrievable in the portal or with az deployment group export. Export template would return the current state including changes made since (the opposite question), decompile converts a local JSON file to Bicep, and tags hold no template.",
    },
    {
        "type": "mc",
        "q": "A CI pipeline must produce the ARM JSON that corresponds to main.bicep so a legacy validation tool can inspect it. Which command performs the conversion?",
        "choices": [
            "az bicep build --file main.bicep",
            "az bicep decompile --file main.bicep",
            "az deployment group export --file main.bicep",
            "az bicep publish --file main.bicep",
        ],
        "answer": 0,
        "explain": "az bicep build transpiles Bicep into ARM JSON — the same deterministic output the deployment commands generate on the fly. Decompile goes the other direction (JSON to Bicep, best-effort), export retrieves a past deployment's template from Azure, and az bicep publish pushes a module to a registry rather than converting formats. Don't reverse the verbs: build = Bicep→JSON, decompile = JSON→Bicep.",
    },
    # --- true/false ---
    {
        "type": "tf",
        "q": "True or False: Redeploying the same ARM template to the same resource group creates duplicate copies of the resources it defines.",
        "answer": False,
        "explain": "Templates are declarative and idempotent: Resource Manager compares the described end state to what exists and creates or updates only as needed, so the same file can be deployed repeatedly with the same result and no duplicates. That safety is exactly what makes templates suitable for nightly CI/CD redeployments.",
    },
    {
        "type": "tf",
        "q": "True or False: To use built-in functions like concat() or uniqueString() in an ARM template, you must first declare them in the template's functions section.",
        "answer": False,
        "explain": "Built-in template functions are always available and need no declaration — the functions section exists only for user-defined functions wrapping complex reusable expressions, and it is rarely used. Confusing built-ins with the functions section is a classic trap on this objective.",
    },
    {
        "type": "tf",
        "q": "True or False: An ARM template's dependsOn array can list a resource that already exists in the resource group but is not defined anywhere in the template.",
        "answer": False,
        "explain": "dependsOn only accepts resources defined in the same template — and a resource that already exists needs no waiting on anyway, since it is already there when deployment starts. Cross-deployment dependencies are handled by referencing existing resources (e.g., Bicep's existing keyword), not by dependsOn.",
    },
    {
        "type": "tf",
        "q": "True or False: If no --mode is specified, az deployment group create runs in Incremental mode, leaving resources that are not in the template unchanged.",
        "answer": True,
        "explain": "Incremental is the default deployment mode: resources in the template are created or updated to match, and anything else in the resource group is left alone — the safe behavior for shared resource groups. Complete mode, which deletes unlisted resources, must be requested explicitly with --mode Complete and only works at resource-group scope.",
    },
    {
        "type": "tf",
        "q": "True or False: The Azure CLI can deploy a .bicep file directly because it installs and manages the Bicep tooling itself.",
        "answer": True,
        "explain": "az deployment commands accept .bicep files directly — the CLI installs and updates Bicep on its own and transpiles to ARM JSON automatically at deployment time. Azure PowerShell also deploys .bicep files, but it expects the Bicep CLI to be installed separately on the machine, a difference the exam likes to probe.",
    },
    # --- fill in ---
    {
        "type": "fill",
        "q": "To convert an exported ARM JSON template into a Bicep file, the admin runs az bicep ______. (type the subcommand)",
        "answers": ["decompile", "az bicep decompile", "bicep decompile"],
        "explain": "az bicep decompile translates ARM JSON into Bicep — a best-effort conversion whose warnings and generated names you should review and tidy. Its counterpart az bicep build goes the other way, deterministically transpiling Bicep into JSON.",
    },
    {
        "type": "fill",
        "q": "The Azure CLI flag added to a deployment command to preview per-resource changes without actually deploying is ______. (type the flag)",
        "answers": ["--what-if", "what-if", "whatif", "--whatif", "-whatif"],
        "explain": "--what-if (CLI) or -WhatIf (PowerShell) runs the what-if operation, reporting Create/Modify/Delete/NoChange for every resource while changing nothing. Use --confirm-with-what-if to show the preview and then prompt before the real deployment proceeds.",
    },
    {
        "type": "fill",
        "q": "The deployment mode that DELETES any resource in the resource group that the template does not define is ______ mode. (type the mode name)",
        "answers": ["complete", "complete mode", "--mode complete"],
        "explain": "Complete mode enforces the template as the whole truth of the resource group and is valid at resource-group scope only — run a partial template with it and everything unlisted is removed. Incremental, the default, is the mode that leaves unlisted resources untouched.",
    },
    {
        "type": "fill",
        "q": "The Bicep keyword that deploys another .bicep file as a nested deployment, passing values through params, is ______. (type the keyword)",
        "answers": ["module"],
        "explain": "A module declaration points at another Bicep file, supplies its params, and can read its output values — the clean replacement for ARM's awkward URI-based nested/linked templates. Remember the module's name property names the nested deployment in the Deployments blade, so two modules with the same name in one deployment collide.",
    },
    {
        "type": "fill",
        "q": "In ARM JSON, the parameter type used for admin passwords so the value stays out of deployment history and logs is ______. (type the type name)",
        "answers": ["securestring", "secure string", "secureString"],
        "explain": "secureString (and secureObject for structured secrets) keeps the value out of deployment history and logs; such parameters should never have a defaultValue and are ideally fed from Azure Key Vault via the parameter file. The Bicep equivalent is the @secure() decorator on the param.",
    },
    # --- matching ---
    {
        "type": "match",
        "q": "Match each ARM template section to its purpose.",
        "pairs": [
            ["parameters", "Values supplied at deployment time, like environment name or SKU"],
            ["variables", "Values computed inside the template to avoid repeating expressions"],
            ["resources", "The resources to deploy or update — the only required section"],
            ["outputs", "Values returned after deployment, like resource IDs and hostnames"],
            ["functions", "User-defined functions for complex reusable expressions"],
        ],
        "explain": "Parameters flow in at deployment time, variables are computed internally, resources (the only required section) declare the end state, outputs flow back to the caller, and functions holds user-defined helpers only — built-ins like concat() never appear there. Knowing which section is required and which holds what is half of this objective's interpretation questions.",
    },
    {
        "type": "match",
        "q": "Match each Bicep keyword or decorator to what it does.",
        "pairs": [
            ["param", "Declares a value supplied at deployment time, with an optional = default"],
            ["var", "Declares a value computed inside the file"],
            ["module", "Deploys another Bicep file as a nested deployment"],
            ["existing", "References an already-deployed resource without redeploying it"],
            ["@secure()", "Keeps a parameter's value out of deployment history and logs"],
            ["@allowed", "Restricts a parameter to a list of permitted values"],
        ],
        "explain": "These are lookup facts the exam tests directly: param/var mirror the JSON parameters/variables sections, module replaces linked templates, existing reads deployed resources, and the decorators @secure() and @allowed mirror secureString and allowedValues. Ordering between resources needs none of these — symbolic references create implicit dependencies for free.",
    },
    {
        "type": "match",
        "q": "Match each Azure CLI command to the task it performs.",
        "pairs": [
            ["az deployment group create", "Deploy a template or Bicep file into an existing resource group"],
            ["az deployment sub create", "Run a subscription-scope deployment that can create resource groups"],
            ["az bicep decompile", "Convert an ARM JSON template into a Bicep file"],
            ["az bicep build", "Transpile a Bicep file into ARM JSON"],
            ["az group export", "Generate a template of a resource group's current state"],
            ["az ts create", "Store a template as a versioned, RBAC-shared template spec"],
        ],
        "explain": "Match the noun to the scope and the verb to the direction: group/sub pick the deployment scope, decompile goes JSON-to-Bicep while build goes Bicep-to-JSON, az group export snapshots current state (best-effort), and az ts create publishes a template spec. Mixing up build and decompile is the classic error this matching drills out.",
    },
    # --- ordering ---
    {
        "type": "order",
        "q": "An admin is modernizing a hand-built resource group into maintainable infrastructure-as-code. Arrange the recommended workflow from first step to last.",
        "items": [
            "Export the resource group's current state as an ARM JSON template",
            "Run az bicep decompile to convert the JSON into Bicep",
            "Refactor the generated code into params, vars, and modules",
            "Run a what-if deployment to confirm the refactor changes nothing",
            "Publish the polished file as a versioned template spec for the organization",
        ],
        "explain": "The standard modernization chain is export → decompile → refactor → verify → share: capture the (imperfect) current-state snapshot, translate it to readable Bicep, clean up the generated names and hard-coded values, prove with what-if that the refactor is a no-op, then publish it as an RBAC-shared template spec. Doing what-if before refactoring, or sharing before verifying, defeats the safety of the workflow.",
    },
    {
        "type": "order",
        "q": "Arrange what happens, from first to last, when an admin runs az deployment group create --resource-group RG1 --template-file main.bicep.",
        "items": [
            "The Azure CLI transpiles the Bicep file into ARM JSON",
            "Resource Manager evaluates the template expressions and validates the template",
            "Resources are created or updated in RG1 to match the declared end state",
            "The deployment is recorded in RG1's Deployments blade with its inputs and outputs",
        ],
        "explain": "Bicep never reaches Azure as Bicep: the CLI transpiles it to ARM JSON first (the same output az bicep build produces), then Resource Manager evaluates expressions, validates, and converges the resource group on the declared state. Every run lands in the Deployments blade — named after the template file unless --name is passed — which is the history you troubleshoot and export from later.",
    },
    {
        "type": "order",
        "q": "Arrange the four template deployment scopes from the BROADEST to the NARROWEST.",
        "items": [
            "Tenant (az deployment tenant create)",
            "Management group (az deployment mg create)",
            "Subscription (az deployment sub create)",
            "Resource group (az deployment group create)",
        ],
        "explain": "Deployments can target four scopes, from tenant at the top, through management groups that organize subscriptions, to subscriptions, down to individual resource groups where most deployments run. Each scope has a matching CLI command and PowerShell cmdlet (New-AzTenantDeployment, New-AzManagementGroupDeployment, New-AzSubscriptionDeployment, New-AzResourceGroupDeployment), and only resource-group scope supports Complete mode.",
    },
]
