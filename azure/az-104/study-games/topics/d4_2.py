TOPIC = {
    "id": "4.2",
    "domain": 4,
    "title": "Configure secure access to virtual networks.",
    "short": "Secure Network Access",
}

TERMS = [
    ("Network security group (NSG)", "Stateful packet filter of allow/deny security rules, attachable to subnets and NICs to control VNet traffic"),
    ("5-tuple", "The five fields every NSG rule matches on: source, source port, destination, destination port, and protocol"),
    ("Rule priority", "Number from 100 to 4096 on custom NSG rules — the LOWER the number, the higher the priority, and processing stops at the first match"),
    ("Default rules", "Built-in NSG rules at priorities 65000-65500 that cannot be deleted, only overridden by lower-numbered custom rules"),
    ("AllowVnetInBound", "Default inbound rule at priority 65000 that allows all traffic from within the VNet and peered VNets"),
    ("AllowAzureLoadBalancerInBound", "Default inbound rule at priority 65001 that allows Azure Load Balancer health probes from 168.63.129.16"),
    ("DenyAllInBound", "Default inbound rule at priority 65500 that denies all traffic not allowed by an earlier rule"),
    ("Service tag", "Microsoft-maintained label (Internet, VirtualNetwork, AzureLoadBalancer, Storage, Sql) used in NSG rules instead of hand-maintained IP ranges"),
    ("168.63.129.16", "The platform source IP of Azure Load Balancer health probes, represented by the AzureLoadBalancer service tag"),
    ("Application security group (ASG)", "Named group of VM NICs used as the source or destination of NSG rules so rules follow app roles instead of IP addresses"),
    ("Stateful rules", "NSG behavior in which reply traffic for an allowed flow is automatically permitted — no rule is needed for return packets"),
    ("Effective security rules", "Read-only view merging the subnet NSG, NIC NSG, and default rules into the combined result actually applied to a NIC"),
    ("Network Watcher", "Regional network diagnostics service hosting effective security rules, IP flow verify, and related troubleshooting tools"),
    ("IP flow verify", "Network Watcher tool that tests whether a specific 5-tuple packet would be allowed or denied and names the exact rule responsible"),
    ("Azure Bastion", "Managed jump service providing browser-based RDP/SSH over TLS 443 through the portal, so VMs need no public IPs"),
    ("AzureBastionSubnet", "The exact required name of Bastion's dedicated subnet, which must be at least /26 in size"),
    ("Bastion Developer SKU", "Free Bastion tier that connects to one VM at a time with no dedicated subnet — for dev/test only"),
    ("Bastion Standard SKU", "Bastion tier adding host scaling (2-50 instances), native-client connections (az network bastion rdp/ssh/tunnel), and file transfer"),
    ("Bastion Premium SKU", "Bastion tier adding session recording and private-only deployment on top of Standard features"),
    ("Service endpoint", "Free per-subnet switch that extends the VNet identity to a PaaS service over the Azure backbone so its firewall can allow that subnet"),
    ("Private endpoint", "A NIC with a private IP from your subnet mapped to one specific PaaS resource, reachable from peered VNets and on-premises"),
    ("Azure Private Link", "The technology behind private endpoints that projects a PaaS resource into your VNet as a private IP address"),
    ("privatelink DNS zone", "Private DNS zone (e.g., privatelink.blob.core.windows.net) linked to the VNet so the service's public name resolves to the private endpoint IP"),
]

QUESTIONS = [
    # --- multiple choice ---
    {
        "type": "mc",
        "q": "An NSG contains a custom Deny rule for TCP 3389 at priority 100 and a custom Allow rule for TCP 3389 at priority 200. An admin tries to RDP to a VM behind this NSG. What happens?",
        "choices": [
            "The connection is allowed because Allow rules always win over Deny rules",
            "The connection is denied because the lower priority number is evaluated first and processing stops at the first match",
            "The connection is allowed because 200 is a higher priority than 100",
            "Azure blocks the deployment because two rules cannot match the same port",
        ],
        "answer": 1,
        "explain": "NSG rules are evaluated strictly by priority number — the LOWER the number, the higher the priority — and processing stops at the first match. The Deny at 100 matches before the Allow at 200 is ever considered. There is no allow-beats-deny logic, and overlapping rules are perfectly legal.",
    },
    {
        "type": "mc",
        "q": "A VM's NIC NSG has an Allow rule for inbound TCP 443, yet HTTPS traffic from the Internet never reaches the VM. The subnet also has an NSG attached. What is the most likely cause?",
        "choices": [
            "The subnet NSG is denying the traffic — inbound traffic is evaluated by the subnet NSG first, and both NSGs must allow it",
            "The NIC NSG is evaluated first inbound, so the subnet NSG is irrelevant",
            "Two NSGs in the same path automatically merge their Allow rules",
            "NSG rules only apply to outbound traffic",
        ],
        "answer": 0,
        "explain": "Inbound traffic hits the subnet NSG first, then the NIC NSG, and BOTH must allow the packet for it to arrive. A NIC-level Allow cannot rescue traffic the subnet NSG already denied. Outbound is the reverse order (NIC first, then subnet), and the NSGs never merge their Allows — each gets a veto.",
    },
    {
        "type": "mc",
        "q": "An admin wants to remove the DenyAllInBound default rule from an NSG because it is blocking a partner's traffic. What should the admin do?",
        "choices": [
            "Delete the default rule from the NSG's rule list",
            "Lower the default rule's priority to 4096",
            "Create a custom Allow rule with a priority lower than 65500, since default rules cannot be deleted",
            "Detach and re-create the NSG without default rules",
        ],
        "answer": 2,
        "explain": "Default rules (priorities 65000-65500) ship with every NSG and can never be deleted or edited. You override them by creating custom rules with lower — therefore stronger — priority numbers (custom range 100-4096). Every NSG always carries its default rules, so re-creation changes nothing.",
    },
    {
        "type": "mc",
        "q": "After tightening an NSG, an admin notices the Azure Load Balancer marks all backend VMs unhealthy. Which change most likely caused this?",
        "choices": [
            "A rule blocked traffic from the AzureLoadBalancer service tag (source 168.63.129.16)",
            "A rule blocked traffic from the VirtualNetwork service tag",
            "The AllowInternetOutBound default rule was deleted",
            "The NSG was associated with too many subnets",
        ],
        "answer": 0,
        "explain": "Azure Load Balancer health probes originate from the platform IP 168.63.129.16, represented by the AzureLoadBalancer service tag, which the default rule at 65001 normally allows. A custom Deny with a lower number that catches this source kills the probes and the pool goes unhealthy. VirtualNetwork covers VNet traffic, not probes, and default rules cannot be deleted.",
    },
    {
        "type": "mc",
        "q": "An admin manages web and database VM tiers whose membership changes weekly. She wants one NSG rule — 'web tier to database tier on TCP 1433' — that never needs editing as VMs come and go. What should she use?",
        "choices": [
            "Separate NSG rules listing each VM's private IP address",
            "Application security groups referenced as the rule's source and destination, with each NIC joined to its tier's ASG",
            "A dedicated subnet per VM with its own NSG",
            "The VirtualNetwork service tag as both source and destination",
        ],
        "answer": 1,
        "explain": "ASGs are named groups of NICs used in place of IP addresses in NSG rules — write 'asg-web to asg-db on 1433' once, then simply join new NICs to the right ASG. IP-based rules need constant maintenance, per-VM subnets do not scale, and the VirtualNetwork tag would allow the whole VNet, not just the two tiers.",
    },
    {
        "type": "mc",
        "q": "An admin writes an NSG rule using asg-web as the source and asg-db as the destination, but rule creation fails. Which constraint was most likely violated?",
        "choices": [
            "ASGs can only be used as a rule's destination, never the source",
            "An NSG rule supports at most one ASG in total",
            "All ASGs referenced in a single rule must contain NICs from the same virtual network",
            "ASG-based rules require the Premium NSG tier",
        ],
        "answer": 2,
        "explain": "Every ASG referenced in one NSG rule must group NICs that live in the same VNet — mixing ASGs whose members span different VNets is not allowed. ASGs work fine as source, destination, or both, and NSGs have no SKU tiers. Remember also that an ASG filters nothing by itself; it only matters when an NSG rule references it.",
    },
    {
        "type": "mc",
        "q": "A VM behind an NSG that allows inbound TCP 443 successfully receives an HTTPS request. Does the admin need an outbound rule so the VM's response can leave?",
        "choices": [
            "Yes — a matching outbound Allow rule is required for the reply",
            "No — NSG rules are stateful, so reply traffic for an allowed flow is automatically permitted",
            "Yes, but only if the subnet and NIC have different NSGs",
            "No — outbound traffic is never filtered by NSGs",
        ],
        "answer": 1,
        "explain": "NSGs are stateful: once a flow is allowed in one direction, its reply traffic is automatically allowed — you never write rules for return packets. Outbound traffic IS filtered (for new outbound flows), so option 4 is wrong; statefulness applies regardless of how many NSGs sit in the path.",
    },
    {
        "type": "mc",
        "q": "A VM has NSGs on both its subnet and its NIC, and an admin needs to see the single combined set of rules actually applied to the VM's network interface. Which feature shows this?",
        "choices": [
            "Effective security rules",
            "IP flow verify",
            "NSG flow logs",
            "Connection monitor",
        ],
        "answer": 0,
        "explain": "Effective security rules is the read-only view that merges the subnet NSG, the NIC NSG, and the default rules into the combined result applied to a NIC — the ground truth when two NSGs are in play. IP flow verify tests one specific packet, flow logs record traffic history, and Connection monitor tracks connectivity over time.",
    },
    {
        "type": "mc",
        "q": "Which Azure CLI command displays the effective (combined) security rules applied to the network interface vm-web1-nic?",
        "choices": [
            "az network nic list-effective-nsg -g rg-net -n vm-web1-nic",
            "az network nsg list -g rg-net",
            "az network nic show-effective-route-table -g rg-net -n vm-web1-nic",
            "az network watcher test-ip-flow --vm vm-web1",
        ],
        "answer": 0,
        "explain": "az network nic list-effective-nsg returns the merged subnet-plus-NIC rule set for a network interface. az network nsg list only enumerates NSG resources, show-effective-route-table shows routes (not security rules), and test-ip-flow evaluates a single 5-tuple rather than listing the combined rules.",
    },
    {
        "type": "mc",
        "q": "Which PowerShell cmdlet retrieves the combined security rules applied to a VM's network interface?",
        "choices": [
            "Get-AzNetworkSecurityGroup",
            "Get-AzEffectiveNetworkSecurityGroup",
            "Get-AzNetworkWatcherFlowLog",
            "Test-AzNetworkWatcherIPFlow",
        ],
        "answer": 1,
        "explain": "Get-AzEffectiveNetworkSecurityGroup (with -NetworkInterfaceName and -ResourceGroupName) returns the merged rules actually enforced on a NIC. Get-AzNetworkSecurityGroup only reads an NSG resource's own rules, flow logs record traffic, and Test-AzNetworkWatcherIPFlow checks a single packet's fate rather than listing all rules.",
    },
    {
        "type": "mc",
        "q": "An admin wants to know whether a specific packet — TCP 3389 from 203.0.113.5 to a VM's private IP — would be allowed, and which exact rule decides it. Which tool answers this?",
        "choices": [
            "Effective security rules",
            "NSG flow logs",
            "Network Watcher IP flow verify",
            "Azure Advisor",
        ],
        "answer": 2,
        "explain": "IP flow verify takes a single 5-tuple question and returns allow/deny plus the name of the rule responsible — perfect for 'would this packet get through?' Effective security rules shows the full merged rule list but does not evaluate a specific packet for you. Flow logs show past traffic and Advisor gives best-practice recommendations.",
    },
    {
        "type": "mc",
        "q": "An admin runs az network nic list-effective-nsg against a deallocated VM's NIC and receives an error. Why?",
        "choices": [
            "The command only works with PowerShell",
            "Effective rules are computed from the live data path, so the VM must be running",
            "The NIC must have a public IP address for the command to work",
            "Effective rules require the NSG to be attached to both the subnet and the NIC",
        ],
        "answer": 1,
        "explain": "Effective security rules (and effective routes) are computed from the live data path of a running VM — a deallocated VM has no active network state to inspect, so the query fails. No public IP is needed, the CLI and PowerShell both work, and a single NSG on either the subnet or the NIC is enough.",
    },
    {
        "type": "mc",
        "q": "An admin is creating the dedicated subnet for an Azure Bastion deployment. Which configuration is valid?",
        "choices": [
            "Name: BastionSubnet, size /27",
            "Name: AzureBastionSubnet, size /28",
            "Name: AzureBastionSubnet, size /26",
            "Name: GatewaySubnet, size /26",
        ],
        "answer": 2,
        "explain": "Bastion's subnet must be named exactly AzureBastionSubnet and be at least /26. A /27 or /28 is too small, any other name is rejected, and GatewaySubnet is the reserved name for VPN/ExpressRoute gateways. These two facts — exact name and /26 minimum — are near-guaranteed exam items.",
    },
    {
        "type": "mc",
        "q": "Admins want to connect to Azure VMs using their locally installed RDP and SSH clients through Bastion via 'az network bastion tunnel'. What is the minimum Bastion SKU that supports this?",
        "choices": ["Developer", "Basic", "Standard", "Premium"],
        "answer": 2,
        "explain": "Native-client connectivity — az network bastion rdp, ssh, and tunnel — starts at the Standard SKU, which also adds host scaling (2-50 instances), file transfer, and shareable links. Developer is the free one-VM-at-a-time tier and Basic offers only browser-based sessions. Premium works too but is not the minimum.",
    },
    {
        "type": "mc",
        "q": "A security team requires that all privileged RDP sessions through Bastion be recorded for audit. Which Bastion SKU is required?",
        "choices": ["Basic", "Standard", "Premium", "Developer"],
        "answer": 2,
        "explain": "Session recording is the flagship Premium-only feature, along with private-only deployment. Standard tops out at native-client support, scaling, and file transfer; Basic is browser-only; Developer is the free single-VM dev/test tier. Map 'record sessions' straight to Premium.",
    },
    {
        "type": "mc",
        "q": "An admin deploying Azure Bastion (Basic SKU) is asked what public IP resources the design needs. Which statement is correct?",
        "choices": [
            "Each target VM needs its own Standard public IP",
            "The Bastion host needs a Standard SKU public IP; the target VMs need no public IPs at all",
            "Neither Bastion nor the VMs need any public IP",
            "Bastion requires a Basic SKU public IP to match its own SKU",
        ],
        "answer": 1,
        "explain": "Bastion removes the need for public IPs on the VMs — it reaches them over their private IPs from inside the VNet — but the Bastion resource itself requires a Standard SKU public IP (Basic public IPs are retired). Only the Developer SKU skips the public IP, and VM-side public IPs defeat the whole purpose.",
    },
    {
        "type": "mc",
        "q": "One Bastion host is deployed in a hub VNet. Can admins use it to reach VMs in spoke VNets?",
        "choices": [
            "No — Bastion can only reach VMs in its own VNet",
            "Yes — Bastion reaches VMs in peered VNets, so a hub Bastion can serve the spokes",
            "Only if every spoke VM has a public IP",
            "Only if each spoke deploys its own AzureBastionSubnet",
        ],
        "answer": 1,
        "explain": "Bastion connects over private IPs and works across VNet peering, so a single hub Bastion can serve VMs in all peered spoke VNets — a cost-efficient hub-and-spoke pattern the exam rewards. Spokes need no Bastion subnets of their own, and public IPs on VMs are exactly what Bastion eliminates.",
    },
    {
        "type": "mc",
        "q": "A storage account must be reachable only from the subnet snet-app, with no additional cost, and the company accepts that the account keeps its public endpoint. What should the admin configure?",
        "choices": [
            "A private endpoint for the storage account in snet-app",
            "A service endpoint for Microsoft.Storage on snet-app, plus a storage firewall rule allowing that subnet",
            "An NSG rule on snet-app denying the Storage service tag",
            "Azure Firewall with a DNAT rule to the storage account",
        ],
        "answer": 1,
        "explain": "'Restrict to a subnet at no cost' is the service-endpoint signature: enable the endpoint on the subnet, then allow that subnet in the storage firewall and deny public access by default. It is a two-sided configuration — doing only one side changes nothing or locks everyone out. Private endpoints cost hourly plus per-GB, an NSG deny blocks the wrong direction, and Azure Firewall is unnecessary here.",
    },
    {
        "type": "mc",
        "q": "A storage account is locked down with a service endpoint to one Azure subnet. Users on the on-premises network, connected via site-to-site VPN, now cannot reach it. Why?",
        "choices": [
            "The VPN tunnel strips the VNet identity headers from packets",
            "Service endpoints work only from Azure VNets — on-premises clients have no subnet identity, so the firewall cannot match them",
            "The service endpoint must also be enabled on the on-premises gateway subnet",
            "Service endpoints block all VPN traffic by design",
        ],
        "answer": 1,
        "explain": "Service endpoints tag traffic with the Azure VNet/subnet identity, which on-premises networks simply do not have — so once the storage firewall denies public access, on-prem traffic has no matching rule. The fixes are allowing the on-prem public IP in the firewall or switching to a private endpoint, which IS reachable over VPN/ExpressRoute.",
    },
    {
        "type": "mc",
        "q": "Auditors require that a storage account have no public endpoint at all and be reachable from on-premises over ExpressRoute using a private IP address. What should the admin deploy?",
        "choices": [
            "A service endpoint for Microsoft.Storage on the app subnet",
            "A storage firewall rule allowing the on-premises public IP range",
            "A private endpoint for the storage account, with public network access disabled",
            "A regional service tag rule (Storage.EastUS) in the NSG",
        ],
        "answer": 2,
        "explain": "'Private IP,' 'no public endpoint,' and 'from on-premises' are the three strongest private-endpoint keywords: Private Link gives the account a NIC with a private IP in your subnet, works over VPN/ExpressRoute and peering, and lets you disable public access entirely. Service endpoints keep the public endpoint and never work from on-premises; firewall or NSG rules still rely on the public endpoint.",
    },
    {
        "type": "mc",
        "q": "After creating a private endpoint for stcontoso and disabling public access, VMs can reach the account by its private IP but connections to stcontoso.blob.core.windows.net fail. What is missing?",
        "choices": [
            "An NSG rule allowing outbound TCP 445 to the Storage tag",
            "The privatelink.blob.core.windows.net private DNS zone linked to the VNet",
            "A service endpoint for Microsoft.Storage on the VMs' subnet",
            "A public DNS CNAME pointing at the private IP",
        ],
        "answer": 1,
        "explain": "Works-by-IP-but-not-by-name is the classic missing-DNS symptom: without the privatelink.blob.core.windows.net zone linked to the VNet, clients keep resolving the public IP, which now refuses connections. The fix is DNS, not firewall rules — create the privatelink zone, link it, and let the CNAME chain land on the endpoint's private IP.",
    },
    {
        "type": "mc",
        "q": "A company wants private-endpoint access for ten different storage accounts from one subnet. How many private endpoints are required?",
        "choices": [
            "One — a private endpoint covers all storage accounts in the subscription",
            "One per region containing a storage account",
            "Ten — a private endpoint maps to exactly one specific resource",
            "Two — one for blob and one for file traffic across all accounts",
        ],
        "answer": 2,
        "explain": "A private endpoint is a NIC mapped to one specific PaaS resource — one storage account, one SQL server, one Key Vault — so ten accounts need ten endpoints (each billed hourly plus per-GB). This per-resource granularity contrasts with a service endpoint, which covers the whole service type for a subnet in one switch.",
    },
    {
        "type": "mc",
        "q": "Which Azure CLI command enables service endpoints for Storage and SQL on the subnet snet-app?",
        "choices": [
            "az network vnet subnet update -g rg-net --vnet-name vnet-hub -n snet-app --service-endpoints Microsoft.Storage Microsoft.Sql",
            "az network private-endpoint create -g rg-net -n snet-app --group-id blob",
            "az network nsg rule create -g rg-net --nsg-name snet-app --service-tags Storage Sql",
            "az storage account update -g rg-net -n snet-app --default-action Deny",
        ],
        "answer": 0,
        "explain": "Service endpoints are a subnet property, so you enable them with az network vnet subnet update --service-endpoints, listing services like Microsoft.Storage and Microsoft.Sql. The private-endpoint command creates a Private Link NIC (a different technology), NSG rules use service tags for packet filtering, and the storage update sets the account firewall — the other half of the configuration.",
    },
    # --- true/false ---
    {
        "type": "tf",
        "q": "True or False: After an NSG allows an inbound flow to a VM, the admin must also create an outbound rule so the VM's replies can leave.",
        "answer": False,
        "explain": "NSGs are stateful: reply traffic for an allowed flow is automatically permitted in the opposite direction. Rules are only written for the initiating direction of a flow — never for return packets. Only new flows initiated outbound need outbound rules.",
    },
    {
        "type": "tf",
        "q": "True or False: A single NSG can be associated with multiple subnets and multiple network interfaces at the same time.",
        "answer": True,
        "explain": "One NSG can serve many subnets and NICs — a reuse pattern the exam rewards because it centralizes rule management instead of duplicating rule sets. The reverse is not true: a given subnet or NIC can have at most one NSG attached.",
    },
    {
        "type": "tf",
        "q": "True or False: Enabling a service endpoint on a subnet also allows on-premises clients connected over site-to-site VPN to pass the PaaS resource's firewall.",
        "answer": False,
        "explain": "Service endpoints work only from within Azure VNets, because the firewall matches on VNet/subnet identity that on-premises networks do not have. On-prem clients must be allowed by public IP or the design must move to a private endpoint, which is reachable over VPN and ExpressRoute.",
    },
    {
        "type": "tf",
        "q": "True or False: Like service endpoints, private endpoints are free of charge.",
        "answer": False,
        "explain": "Service endpoints are free, but private endpoints carry an hourly charge per endpoint plus a per-GB data-processing charge. Cost is one of the standard discriminators between the two: 'no additional cost' points to service endpoints, while the stronger isolation of Private Link is paid for.",
    },
    {
        "type": "tf",
        "q": "True or False: The Azure Bastion Developer SKU is free and connects to one VM at a time, making it suitable for dev/test scenarios.",
        "answer": True,
        "explain": "Developer is the free entry tier: one concurrent VM connection, limited regions, and no dedicated subnet or public IP requirement. Production features climb the ladder — Basic for dedicated browser-based access, Standard for native clients and scaling, Premium for session recording.",
    },
    # --- fill in ---
    {
        "type": "fill",
        "q": "The dedicated subnet for an Azure Bastion deployment must be named exactly ______. (type the subnet name)",
        "answers": ["azurebastionsubnet", "azure bastion subnet"],
        "explain": "The name AzureBastionSubnet is mandatory and exact, and the subnet must be at least /26. Only Bastion hosts live there — you don't place VMs in it. These two rote facts appear on nearly every AZ-104 form.",
    },
    {
        "type": "fill",
        "q": "Azure Load Balancer health probes originate from the platform IP address ______, represented by the AzureLoadBalancer service tag. (type the IP)",
        "answers": ["168.63.129.16"],
        "explain": "168.63.129.16 is the fixed platform source for load balancer health probes (and other host services); the default rule AllowAzureLoadBalancerInBound at 65001 admits it. When probes fail after an NSG change, look for a custom Deny that catches this source.",
    },
    {
        "type": "fill",
        "q": "For a private endpoint to Azure Blob Storage to resolve by name inside the VNet, an admin creates the private DNS zone named ______ and links it to the VNet. (type the zone name)",
        "answers": ["privatelink.blob.core.windows.net", "privatelink.blob.core.windows.net."],
        "explain": "Each Private Link service has a matching privatelink.* zone — privatelink.blob.core.windows.net for blob, privatelink.database.windows.net for Azure SQL. Azure inserts a CNAME from the public name through the privatelink name to the endpoint's private IP, but only clients using the linked zone see it.",
    },
    {
        "type": "fill",
        "q": "The lowest (strongest) priority number an admin can assign to a custom NSG rule is ______. (type the number)",
        "answers": ["100"],
        "explain": "Custom rule priorities run from 100 to 4096, with lower numbers evaluated first and processing stopping at the first match. The 65000-65500 range belongs to the undeletable default rules, which any custom rule outranks.",
    },
    {
        "type": "fill",
        "q": "The Network Watcher tool that tests whether a specific 5-tuple packet would be allowed to or from a VM, and names the rule responsible, is IP ______ verify. (type the missing word)",
        "answers": ["flow", "ip flow verify", "flow verify"],
        "explain": "IP flow verify answers a single-packet question — 'would TCP 3389 from 203.0.113.5 be allowed?' — and identifies the exact NSG rule that allowed or denied it. Effective security rules is its sibling tool for viewing the full merged rule set on a NIC.",
    },
    # --- matching ---
    {
        "type": "match",
        "q": "Match each default NSG rule to its priority and effect.",
        "pairs": [
            ["AllowVnetInBound", "65000 — allows all inbound traffic from the VNet and peered VNets"],
            ["AllowAzureLoadBalancerInBound", "65001 — allows health probes from 168.63.129.16"],
            ["DenyAllInBound", "65500 — denies all remaining inbound traffic"],
            ["AllowInternetOutBound", "65001 outbound — allows all traffic to the Internet"],
        ],
        "explain": "The default rules occupy 65000-65500 in each direction and cannot be deleted, only overridden by custom rules at 100-4096. Inbound: VNet traffic and load balancer probes are allowed before the final DenyAll; outbound: VNet and Internet traffic are allowed before DenyAllOutBound.",
    },
    {
        "type": "match",
        "q": "Match each Azure Bastion SKU to its defining feature.",
        "pairs": [
            ["Developer", "Free tier connecting to one VM at a time for dev/test"],
            ["Basic", "Dedicated deployment with browser-based RDP/SSH only"],
            ["Standard", "Native-client connections, host scaling 2-50, and file transfer"],
            ["Premium", "Session recording and private-only deployment"],
        ],
        "explain": "The SKU ladder maps cleanly to keywords: 'free, single VM' is Developer; browser-only dedicated hosts are Basic; 'native client,' 'az network bastion tunnel,' or scaling means Standard; and 'record sessions' means Premium. Each tier includes everything below it.",
    },
    {
        "type": "match",
        "q": "Match each requirement to the technology that satisfies it.",
        "pairs": [
            ["Restrict a storage account to one subnet at no extra cost", "Service endpoint"],
            ["Give a PaaS resource a private IP and disable its public endpoint", "Private endpoint"],
            ["RDP to VMs in the browser over 443 with no public IPs on the VMs", "Azure Bastion"],
            ["View the combined subnet + NIC rules applied to a VM", "Effective security rules"],
            ["Test whether one specific packet would be allowed and name the rule", "IP flow verify"],
        ],
        "explain": "Sort by the requirement's strongest keyword: 'no cost / restrict to subnet' is a service endpoint; 'private IP / no public endpoint / from on-premises' is a private endpoint; browser RDP without VM public IPs is Bastion; 'combined rules' is effective security rules; and single-packet verdicts come from IP flow verify.",
    },
    # --- ordering ---
    {
        "type": "order",
        "q": "Arrange the evaluation path of an INBOUND packet destined for a VM whose subnet and NIC each have an NSG, from first check to delivery.",
        "items": [
            "Subnet NSG custom rules are evaluated in priority order (lowest number first)",
            "Subnet NSG default rules (65000-65500) apply if no custom rule matched",
            "NIC NSG custom rules are evaluated in priority order",
            "NIC NSG default rules apply if no custom rule matched",
            "The packet is delivered to the VM only because both NSGs allowed it",
        ],
        "explain": "Inbound order is subnet NSG first, then NIC NSG, and within each NSG rules run by ascending priority number with processing stopping at the first match; unmatched traffic falls through to the default rules. Both NSGs must allow the packet — either one can veto it. Outbound reverses the NSG order: NIC first, then subnet.",
    },
    {
        "type": "order",
        "q": "Arrange the steps to deploy Azure Bastion (Standard SKU) into an existing VNet, from first to last.",
        "items": [
            "Create a subnet named AzureBastionSubnet with at least a /26 prefix",
            "Create a Standard SKU public IP address for the Bastion host",
            "Create the Bastion resource in the VNet referencing that public IP",
            "Connect to VMs over their private IPs via the portal or native client",
        ],
        "explain": "Bastion needs its exactly-named AzureBastionSubnet (/26 minimum) and a Standard SKU public IP before the resource itself can be created — az network bastion create references both. Once deployed, admins reach VMs by private IP through the browser, or with native clients on the Standard SKU and above; the VMs themselves need no public IPs.",
    },
    {
        "type": "order",
        "q": "Arrange the steps to give a storage account fully private access with working name resolution, from first to last.",
        "items": [
            "Create a private endpoint in the subnet mapped to the storage account's blob service",
            "Create the privatelink.blob.core.windows.net private DNS zone",
            "Link the private DNS zone to the virtual network",
            "Create a DNS zone group so the endpoint's A record is registered in the zone",
            "Disable public network access on the storage account",
        ],
        "explain": "The private endpoint supplies the private IP, but clients only use it once DNS resolves the account name privately — hence the privatelink zone, the VNet link, and the zone group that writes the endpoint's A record. Disabling public access last avoids an outage window in which clients still resolve the public IP but are refused.",
    },
]
