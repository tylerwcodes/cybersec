TOPIC = {
    "id": "3.3",
    "domain": 3,
    "title": "Provision and manage containers in the Azure portal.",
    "short": "Containers (ACR/ACI/ACA)",
}

TERMS = [
    ("Azure Container Registry (ACR)", "Managed private Docker/OCI registry for storing and distributing container images and artifacts, with Entra-integrated auth"),
    ("Login server", "The registry's DNS endpoint at <name>.azurecr.io; the registry name must be globally unique, 5-50 alphanumeric characters"),
    ("ACR Premium SKU", "The only registry tier offering geo-replication, private endpoints/Private Link, and customer-managed keys, plus the highest storage/throughput"),
    ("Geo-replication", "Premium ACR feature that serves one registry from multiple regions so images are pulled from a nearby replica"),
    ("ACR admin account", "Single shared username/password on a registry — disabled by default and discouraged because it has no per-user audit trail"),
    ("Service principal", "Application identity granted an RBAC role such as AcrPush or AcrPull — the recommended auth for CI/CD pipelines and headless services"),
    ("AcrPull / AcrPush", "Built-in RBAC roles granting image pull-only or push-and-pull rights on a container registry"),
    ("Repository-scoped token", "ACR credential bound to specific repositories with specific actions — for granting an outsider access to one repo, not the whole registry"),
    ("az acr build", "ACR Tasks 'quick task' that builds an image in Azure from local source and pushes it — no local Docker daemon needed"),
    ("az acr import", "Command that copies an existing image registry-to-registry (e.g., Docker Hub into ACR) without a local docker pull/push"),
    ("ACR Tasks", "Registry automation that rebuilds images on source-code commit or base-image update, plus scheduled and multi-step tasks"),
    ("Azure Container Instances (ACI)", "Fastest serverless way to run a container — no VM or orchestrator, billed per second for requested vCPU and memory"),
    ("Container group", "ACI's unit of deployment: containers on the same host sharing a lifecycle, network (one IP/port space), and volumes — like a Kubernetes pod"),
    ("Restart policy", "ACI setting of Always (default), OnFailure, or Never — Never makes a container run once to completion and stop"),
    ("DNS name label", "ACI setting giving a public container a name like <label>.<region>.azurecontainer.io"),
    ("Azure Files volume mount", "The way to persist ACI data — container filesystems are ephemeral, but a mounted Azure Files share survives restarts"),
    ("Secure environment variables", "ACI secrets passed with --secure-environment-variables so values are hidden from the portal and az container show"),
    ("Azure Container Apps", "Serverless container platform on managed Kubernetes with KEDA, Dapr, and Envoy built in — no cluster to manage, no Kubernetes API access"),
    ("Container Apps environment", "Secure boundary around container apps — apps inside share a virtual network and a Log Analytics workspace"),
    ("Revision", "Immutable snapshot of a container app's image and configuration created by each deployment; multiple revisions enable traffic splitting"),
    ("Traffic splitting", "Weighting requests across revisions (e.g., 90/10) for blue-green and canary releases without extra infrastructure"),
    ("Ingress", "Container Apps' built-in Envoy-backed HTTPS endpoint — external (internet-facing) or internal (environment/VNet only) with no load balancer to build"),
    ("Consumption plan", "Container Apps billing that charges per second for what replicas use — and nothing when an app sits at zero replicas"),
    ("Dedicated workload profiles", "Container Apps plan offering reserved, larger compute (including specialized sizes) for predictable or resource-hungry apps"),
    ("KEDA", "Kubernetes Event-Driven Autoscaling built into Container Apps — scales replicas on queue length, Event Hubs lag, Kafka, and dozens of other event sources"),
    ("Dapr", "Distributed Application Runtime — managed sidecar giving container apps service invocation, state stores, and pub/sub without SDK plumbing"),
    ("Envoy", "The ingress proxy built into Container Apps that handles TLS termination and traffic splitting"),
    ("Container Apps jobs", "Containers that run to completion on a manual, scheduled (cron), or event-driven trigger — the Container Apps answer to finite batch work"),
    ("Scale to zero", "Container Apps behavior where an idle app drops to zero replicas and zero compute cost — requires min replicas 0 with an HTTP or KEDA event rule"),
    ("Azure Kubernetes Service (AKS)", "Full managed Kubernetes with kubectl access and cluster-level control — the step up when Container Apps' abstractions aren't enough"),
]

QUESTIONS = [
    # --- multiple choice ---
    {
        "type": "mc",
        "q": "A global company wants its container registry's images replicated to three Azure regions so AKS clusters in each region pull from a nearby replica of the SAME registry. Which ACR SKU is required?",
        "choices": ["Basic", "Standard", "Premium", "Standard with zone-redundant storage enabled"],
        "answer": 2,
        "explain": "Geo-replication — one registry served from multiple regions — is exclusive to the Premium SKU, along with private endpoints and customer-managed keys. Basic and Standard differ from Premium mainly in storage/throughput and lack all three of those features; zone redundancy is not a substitute for multi-region replication.",
    },
    {
        "type": "mc",
        "q": "An existing Standard-SKU registry must now be reachable only over a private endpoint to satisfy a new security policy. What is the most direct fix?",
        "choices": [
            "Run az acr update --sku Premium, then add the private endpoint",
            "Delete the registry and recreate it as Basic with a service endpoint",
            "Enable the admin account and restrict its password distribution",
            "Run az acr import to move the images into a new Standard registry in a VNet",
        ],
        "answer": 0,
        "explain": "Private endpoints/Private Link require the Premium SKU, and a registry can be upgraded in place with az acr update --sku Premium — no recreation needed. Basic has fewer features than Standard, the admin account is an authentication credential (not network isolation), and Standard registries cannot use private endpoints regardless of where you import images.",
    },
    {
        "type": "mc",
        "q": "An Azure DevOps pipeline must push freshly built images to ACR on every merge. What is the RECOMMENDED way to authenticate the pipeline?",
        "choices": [
            "Enable the registry admin account and store its password as a pipeline secret",
            "Use a service principal (or managed identity) granted the AcrPush role",
            "Have an engineer run az acr login at the start of each release",
            "Issue a repository-scoped token with read-only actions",
        ],
        "answer": 1,
        "explain": "Headless automation should use a service principal or managed identity with an RBAC role — AcrPush for pushing images — giving per-identity control and auditability. The admin account is a single shared credential, disabled by default and discouraged; az acr login is interactive and tied to a human's Entra ID; a read-only token cannot push.",
    },
    {
        "type": "mc",
        "q": "A partner company needs to PULL images from exactly one repository in your registry and must not see or touch anything else. What should you give them?",
        "choices": [
            "The admin account username and password",
            "A service principal with AcrPull on the whole registry",
            "A repository-scoped token limited to that repository with pull actions",
            "An Entra guest account with the Reader role on the resource group",
        ],
        "answer": 2,
        "explain": "Repository-scoped tokens bind a credential to specific repositories with specific actions — precisely the 'one repo, pull only' case. The admin account and a registry-wide AcrPull assignment both expose every repository, and the ARM Reader role governs the Azure resource, not registry data-plane pulls.",
    },
    {
        "type": "mc",
        "q": "A developer on a locked-down laptop with NO Docker installed has a Dockerfile and source code that must become an image in ACR. Which single command accomplishes this?",
        "choices": [
            "az acr import --name conregistry01 --source . --image webapp:v1",
            "az acr build --registry conregistry01 --image webapp:v1 .",
            "docker build -t conregistry01.azurecr.io/webapp:v1 . && docker push",
            "az container create --image webapp:v1 --registry-login-server conregistry01.azurecr.io",
        ],
        "answer": 1,
        "explain": "az acr build uploads the build context and runs the build in Azure as an ACR Tasks quick task, pushing the result to the registry — no local Docker daemon required. az acr import copies an already-built image between registries, docker build needs Docker installed, and az container create runs an existing image rather than building one.",
    },
    {
        "type": "mc",
        "q": "You need the public Docker Hub image nginx:latest available inside your ACR, but corporate policy forbids pulling images to admin workstations. Which command fits?",
        "choices": [
            "az acr build --registry conregistry01 --image nginx:latest .",
            "docker pull nginx && docker push conregistry01.azurecr.io/nginx",
            "az acr import --name conregistry01 --source docker.io/library/nginx:latest --image nginx:latest",
            "az acr repository list --name conregistry01",
        ],
        "answer": 2,
        "explain": "az acr import copies an image registry-to-registry entirely in the cloud, so nothing touches a local machine. az acr build compiles from source (there is no source here), the docker pull/push pair violates the policy, and az acr repository list only inspects existing registry contents.",
    },
    {
        "type": "mc",
        "q": "A security team wants application images rebuilt automatically whenever the underlying Linux base image is patched upstream. Which feature provides this?",
        "choices": [
            "An ACR Tasks base-image update trigger",
            "A registry webhook posting to a Teams channel",
            "An Azure Automation runbook polling Docker Hub nightly",
            "Geo-replication with the Premium SKU",
        ],
        "answer": 0,
        "explain": "ACR Tasks can trigger builds on source-code commits AND on base-image updates, so a patched OS/framework image automatically rebuilds every dependent application image. A webhook only notifies (it doesn't rebuild), a polling runbook is custom plumbing you'd have to build and maintain, and geo-replication addresses image distribution, not rebuilds.",
    },
    {
        "type": "mc",
        "q": "A data team needs to run a one-off Linux container for a few hours with a public IP as quickly and simply as possible, paying only for the seconds it runs, with no orchestration. Which service fits?",
        "choices": ["Azure Kubernetes Service", "Azure Container Apps", "Azure Container Instances", "Azure App Service"],
        "answer": 2,
        "explain": "ACI is the fastest, simplest serverless way to run a container: no VM, no orchestrator, per-second billing from creation to termination, and an optional public IP with a DNS label. AKS and Container Apps add orchestration and platform layers this job doesn't need, and App Service is a web-app PaaS rather than the quickest single-container host.",
    },
    {
        "type": "mc",
        "q": "A nightly ETL container in ACI must run to completion, stop, and NOT be restarted by the platform — and should only be billed while it runs. Which restart policy do you set?",
        "choices": ["Always", "OnFailure", "Never", "Manual"],
        "answer": 2,
        "explain": "Restart policy Never is for run-once jobs: the container executes, exits, the group stops, and per-second billing stops with it. Always (the default) keeps restarting the container as if it were a service, OnFailure restarts only on nonzero exit codes, and Manual is not an ACI restart policy.",
    },
    {
        "type": "mc",
        "q": "An admin wants a logging sidecar container deployed in the SAME ACI container group as the application container. Which constraint applies?",
        "choices": [
            "The group must use Windows containers, which support sidecars natively",
            "Multi-container groups are supported only for Linux containers",
            "Sidecars require an AKS cluster; ACI groups hold one container",
            "Each container in the group needs its own public IP address",
        ],
        "answer": 1,
        "explain": "Multi-container groups — the sidecar pattern — are Linux-only in ACI; Windows container groups support only a single container. ACI groups can absolutely hold multiple (Linux) containers without AKS, and the whole group shares one IP address and port space, like a Kubernetes pod.",
    },
    {
        "type": "mc",
        "q": "A report generated inside an ACI container disappears every time the container stops. What is the correct fix?",
        "choices": [
            "Attach a Premium SSD managed disk to the container group",
            "Change the storage redundancy of the container group to GRS",
            "Mount an Azure Files share as a volume and write the report there",
            "Set the restart policy to Always so the container never stops",
        ],
        "answer": 2,
        "explain": "Container filesystems are ephemeral — anything written inside vanishes when the container stops. Mounting an Azure Files share as a volume persists data across restarts (for Linux containers). Managed disks attach to VMs, not ACI groups; redundancy settings belong to storage accounts; and Always merely restarts the container, still losing its local writes.",
    },
    {
        "type": "mc",
        "q": "You deploy a public ACI container with the DNS name label 'webapp-demo' in East US. Which FQDN will reach it?",
        "choices": [
            "webapp-demo.eastus.azurecontainer.io",
            "webapp-demo.azurecr.io",
            "webapp-demo.eastus.cloudapp.azure.com",
            "webapp-demo.azurewebsites.net",
        ],
        "answer": 0,
        "explain": "ACI DNS name labels resolve as <label>.<region>.azurecontainer.io. The azurecr.io suffix belongs to Container Registry login servers, cloudapp.azure.com to classic/VM public IP DNS labels, and azurewebsites.net to App Service — all plausible-looking but wrong domains.",
    },
    {
        "type": "mc",
        "q": "An admin must deploy an ACI group containing an app container plus a monitoring sidecar using the Azure CLI. How is a multi-container group defined?",
        "choices": [
            "Run az container create twice with the same group name",
            "Supply a YAML file: az container create --file group.yaml (or use an ARM/Bicep template)",
            "Pass both images with repeated --image flags on one az container create",
            "Create each container in the portal and link them afterward",
        ],
        "answer": 1,
        "explain": "The portal and basic az container create parameters deploy single-container groups; multi-container (sidecar) groups are defined in a YAML file passed with --file, or in an ARM/Bicep template. Running the command twice replaces rather than extends the group, --image is not repeatable for this purpose, and the portal cannot link containers into an existing group.",
    },
    {
        "type": "mc",
        "q": "A database connection string must be passed to an ACI container, but it must NOT appear in the portal or in az container show output. How do you pass it?",
        "choices": [
            "With --environment-variables on az container create",
            "With --secure-environment-variables on az container create",
            "Base64-encode it and pass it with --environment-variables",
            "Write it into the container image at build time",
        ],
        "answer": 1,
        "explain": "Secure environment variables are hidden from the portal and from az container show output, making them the right channel for secrets. Regular environment variables are fully visible, base64 is encoding rather than protection, and baking secrets into images exposes them to anyone who can pull the image.",
    },
    {
        "type": "mc",
        "q": "A run-once ACI container exited unexpectedly and the group has stopped. Which command shows the container's stdout/stderr output so you can diagnose it?",
        "choices": [
            "az container exec --resource-group rg-app --name nightlyjob --exec-command '/bin/sh'",
            "az container logs --resource-group rg-app --name nightlyjob",
            "az acr repository list --name conregistry01",
            "az container show --resource-group rg-app --name nightlyjob",
        ],
        "answer": 1,
        "explain": "az container logs retrieves the container's log output and works even after the container has stopped. az container exec opens an interactive shell, which requires a running container; az acr repository list inspects registry contents; and az container show returns resource properties and state, not application output.",
    },
    {
        "type": "mc",
        "q": "Which PowerShell cmdlet creates an Azure Container Instances container group?",
        "choices": ["New-AzContainerRegistry", "New-AzContainerInstance", "New-AzContainerGroup", "Start-AzContainerGroup"],
        "answer": 2,
        "explain": "The Az.ContainerInstance module manages ACI at the container-group level: New-AzContainerGroup creates one, with Get- and Remove-AzContainerGroup alongside. New-AzContainerRegistry creates an ACR registry (Az.ContainerRegistry module), and the other names are plausible but not the creation cmdlet for a group.",
    },
    {
        "type": "mc",
        "q": "Three microservice container apps must share one virtual network and send their logs to the same Log Analytics workspace. How do you arrange this in Azure Container Apps?",
        "choices": [
            "Deploy all three apps into the same Container Apps environment",
            "Place all three apps in the same resource group",
            "Run all three containers as one revision of a single app",
            "Assign all three apps to the same Dedicated workload profile",
        ],
        "answer": 0,
        "explain": "The environment is the secure boundary in Container Apps: apps within one environment share a virtual network and write logs to the same Log Analytics workspace. A resource group is only a management grouping with no network implications, a revision is a snapshot of one app, and workload profiles allocate compute rather than networking or logging.",
    },
    {
        "type": "mc",
        "q": "After shipping v2 of an API on Azure Container Apps, the team wants 10% of production traffic on v2 and 90% on v1 — without deploying any load balancer. What do you use?",
        "choices": [
            "An Application Gateway with weighted backend pools",
            "Azure Traffic Manager weighted routing profiles",
            "Multiple-revision mode with traffic splitting between revisions",
            "A second container app behind Azure Front Door",
        ],
        "answer": 2,
        "explain": "Every Container Apps deployment creates an immutable revision, and multiple-revision mode lets you split traffic by percentage across revisions — canary and blue-green with zero extra infrastructure, because the built-in Envoy ingress does the weighting. Application Gateway, Traffic Manager, and Front Door could approximate this but add cost and components the platform already provides.",
    },
    {
        "type": "mc",
        "q": "A platform team insists on kubectl access, custom resource definitions, and full control of cluster upgrades for their container workloads. Which service must they use?",
        "choices": ["Azure Container Apps", "Azure Container Instances", "Azure Kubernetes Service", "Azure App Service"],
        "answer": 2,
        "explain": "Container Apps runs on managed Kubernetes but deliberately hides the Kubernetes API — no kubectl, no CRDs, no cluster-level control. When a scenario demands those, the answer steps up to AKS. ACI has no cluster at all, and App Service is not a Kubernetes platform.",
    },
    {
        "type": "mc",
        "q": "A worker container app must add replicas as the backlog in an Azure Service Bus queue grows, not when CPU rises. Which scale rule type fits?",
        "choices": [
            "A CPU utilization scale rule",
            "A KEDA event-driven scale rule on queue length",
            "An HTTP concurrency scale rule",
            "A memory utilization scale rule",
        ],
        "answer": 1,
        "explain": "KEDA scalers react to the event backlog itself — Storage or Service Bus queue length, Event Hubs lag, Kafka, and dozens more — which is exactly right for queue-driven work. CPU and memory rules measure the replica, not the queue, so they lag behind the real demand; HTTP rules measure concurrent web requests, which a queue worker doesn't receive.",
    },
    {
        "type": "mc",
        "q": "A container app must incur ZERO compute cost during idle overnight hours. Which configuration achieves this?",
        "choices": [
            "min-replicas 0 with an HTTP or KEDA event-driven scale rule",
            "min-replicas 0 with a CPU utilization scale rule",
            "min-replicas 1 on the Consumption plan",
            "A Dedicated workload profile with autoscale enabled",
        ],
        "answer": 0,
        "explain": "Scale to zero requires min replicas of 0 combined with an HTTP or KEDA event trigger — at zero replicas the Consumption plan charges nothing. CPU and memory rules cannot reach zero because they need at least one running replica to measure, min-replicas 1 always keeps one replica billing, and Dedicated profiles reserve compute you pay for regardless.",
    },
    {
        "type": "mc",
        "q": "An ACI container group serving an internal API is overwhelmed at month-end peaks and idle otherwise. What is the BEST course of action?",
        "choices": [
            "Enable the container group's autoscale setting for peak hours",
            "Resize the running group to 8 vCPU and 32 GB of memory",
            "Migrate the workload to Azure Container Apps with scale rules",
            "Add a KEDA scaler directly to the container group",
        ],
        "answer": 2,
        "explain": "ACI container groups are fixed-size with no autoscaling — there is no autoscale setting and KEDA does not attach to ACI. The standard group cap is around 4 vCPU / 16 GB, so 8/32 isn't available, and resizing requires redeployment anyway. Fluctuating demand is the signal to move to Container Apps, which scales on HTTP, events, or CPU/memory.",
    },
    # --- true/false ---
    {
        "type": "tf",
        "q": "True or False: An Azure Container Instances container group automatically adds instances when its CPU utilization climbs.",
        "answer": False,
        "explain": "ACI has no autoscaling of any kind — a container group is a fixed-size allocation for its lifetime. Handling more load means deploying additional container groups yourself (or via automation you build), or moving the workload to Azure Container Apps, which does autoscale.",
    },
    {
        "type": "tf",
        "q": "True or False: The ACR admin account is enabled by default when you create a new registry.",
        "answer": False,
        "explain": "The admin account is disabled by default and stays discouraged: it is one shared username/password with full push/pull rights and no per-user audit trail. Recommended alternatives are az acr login for humans, service principals or managed identities with AcrPush/AcrPull for automation, and repository-scoped tokens for narrow external access.",
    },
    {
        "type": "tf",
        "q": "True or False: Azure Container Instances bills per second for the vCPU and memory you request, from creation to termination.",
        "answer": True,
        "explain": "Per-second billing on requested resources is a defining ACI trait — a run-once container with restart policy Never is billed only while it actually runs. This is why ACI is the answer for short-lived jobs where paying for an always-on VM or cluster would be wasteful.",
    },
    {
        "type": "tf",
        "q": "True or False: A Container Apps scale rule based on CPU or memory utilization can scale the app down to zero replicas.",
        "answer": False,
        "explain": "CPU and memory rules need at least one running replica to produce a measurement, so apps using them floor at one replica. Scale to zero — and the zero compute cost that comes with it — requires min replicas 0 with an HTTP or KEDA event-driven rule instead.",
    },
    {
        "type": "tf",
        "q": "True or False: Running az acr build performs the image build in Azure, so the machine issuing the command does not need a Docker daemon.",
        "answer": True,
        "explain": "az acr build is an ACR Tasks quick task: the build context is uploaded, built in the cloud, and the finished image is pushed into the registry. That makes it the answer whenever a stem says 'build and push without Docker installed locally' — distinct from az acr import, which copies an already-built image between registries.",
    },
    # --- fill in ---
    {
        "type": "fill",
        "q": "To copy an existing image from Docker Hub into your registry without pulling it locally, you run: az acr ______ (type the subcommand)",
        "answers": ["import"],
        "explain": "az acr import performs a registry-to-registry copy entirely in the cloud — for example from docker.io/library/nginx:latest into your ACR. Contrast with az acr build, which builds a new image from source code, also without a local Docker daemon.",
    },
    {
        "type": "fill",
        "q": "The event-driven autoscaling technology built into Azure Container Apps, which can scale replicas on queue length or Event Hubs lag, is ______. (type your answer)",
        "answers": ["keda", "kubernetes event-driven autoscaling", "kubernetes event driven autoscaling"],
        "explain": "KEDA (Kubernetes Event-Driven Autoscaling) ships inside Container Apps along with Dapr and Envoy. Its dozens of scalers react to the actual backlog — Storage/Service Bus queue length, Kafka, Event Hubs — and combined with min replicas 0 they enable scale to zero.",
    },
    {
        "type": "fill",
        "q": "The ACI restart policy that makes a batch container run to completion once, stop, and never be restarted by the platform is ______. (type your answer)",
        "answers": ["never", "restart-policy never", "--restart-policy never"],
        "explain": "Never is the run-once policy: the container executes, exits, and per-second billing ends with it. Always (the default) treats the container as a service that must stay up, and OnFailure restarts only when the container exits with an error code.",
    },
    {
        "type": "fill",
        "q": "The managed sidecar runtime in Azure Container Apps that provides service-to-service invocation, state stores, and pub/sub without SDK plumbing is ______. (type your answer)",
        "answers": ["dapr", "distributed application runtime"],
        "explain": "Dapr (Distributed Application Runtime) can be enabled per app and runs as a managed sidecar, giving microservices common building blocks through a consistent API. It is one of the three open-source engines built into Container Apps, next to KEDA for event-driven scale and Envoy for ingress.",
    },
    {
        "type": "fill",
        "q": "A registry named conregistry01 is reachable at the login server conregistry01.______ (type the DNS suffix)",
        "answers": ["azurecr.io", ".azurecr.io"],
        "explain": "Every ACR registry gets a login server at <name>.azurecr.io, which is why the registry name must be globally unique and 5-50 alphanumeric characters. Don't confuse it with azurecontainer.io, the suffix ACI uses for public container DNS name labels.",
    },
    # --- matching ---
    {
        "type": "match",
        "q": "Match each ACR authentication method to the scenario it fits best.",
        "pairs": [
            ["az acr login", "A developer interactively pushing images with their Entra ID identity"],
            ["Service principal with AcrPush", "A CI/CD pipeline pushing images headlessly"],
            ["Repository-scoped token", "A partner needing pull access to a single repository only"],
            ["Admin account", "A quick test with basic auth — disabled by default and discouraged"],
        ],
        "explain": "Match auth to actor: interactive humans use az acr login (their Entra identity wrapped for Docker), automation uses a service principal or managed identity with AcrPush/AcrPull, narrow external access uses a repository-scoped token, and the shared admin account is a last resort with no per-user audit trail.",
    },
    {
        "type": "match",
        "q": "Match each container service to the scenario that calls for it.",
        "pairs": [
            ["Azure Container Registry", "A private Azure-native store for Docker/OCI images"],
            ["Azure Container Instances", "A single run-once container, fastest start, per-second billing"],
            ["Azure Container Apps", "Microservices that autoscale on events and can scale to zero"],
            ["Azure Kubernetes Service", "Workloads demanding kubectl and full cluster control"],
        ],
        "explain": "ACR stores images; the other three run them at increasing levels of capability and operational burden. ACI is the simplest single-container host, Container Apps adds revisions, ingress, and KEDA/HTTP autoscale without exposing Kubernetes, and AKS hands you the whole cluster when you truly need the Kubernetes API.",
    },
    {
        "type": "match",
        "q": "Match each Azure Container Apps concept to its description.",
        "pairs": [
            ["Environment", "Secure boundary whose apps share a VNet and Log Analytics workspace"],
            ["Revision", "Immutable snapshot of an app's image and configuration per deployment"],
            ["Ingress", "Built-in Envoy HTTPS endpoint, external or internal, no load balancer to build"],
            ["Consumption plan", "Per-second billing that drops to nothing at zero replicas"],
            ["Container Apps job", "A container run to completion on a manual, cron, or event trigger"],
        ],
        "explain": "The environment is the architectural anchor (network + logging boundary), revisions make deployments versioned and splittable, ingress provides managed HTTPS entry, the Consumption plan is what makes scale-to-zero free, and jobs handle finite batch work that an always-on app model doesn't fit.",
    },
    # --- ordering ---
    {
        "type": "order",
        "q": "Arrange the steps to take an application from source code to a running Azure Container Instance, first to last.",
        "items": [
            "Create the registry with az acr create",
            "Build and push the image in the cloud with az acr build",
            "Deploy the image with az container create, supplying AcrPull credentials",
            "Verify the container's output with az container logs",
        ],
        "explain": "The registry must exist before az acr build can push into it, the image must exist in the registry before az container create can pull it (using a service principal or managed identity with AcrPull), and az container logs confirms the running container's output. This registry-then-image-then-instance chain is the standard ACR-to-ACI workflow.",
    },
    {
        "type": "order",
        "q": "Arrange the steps of a canary release on Azure Container Apps, first to last.",
        "items": [
            "Create the environment with az containerapp env create",
            "Deploy v1 with az containerapp create, enabling external ingress",
            "Push v2 with az containerapp update, creating a new revision",
            "Split traffic 90/10 with az containerapp ingress traffic set",
            "After validation, shift 100% of traffic to the v2 revision",
        ],
        "explain": "The environment is created first because every app must deploy into one. The initial create establishes the app and its ingress; each subsequent update produces a new immutable revision; and in multiple-revision mode, traffic weights route a small canary share to v2 before you promote it to 100%. No external load balancer is involved — Envoy ingress handles the split.",
    },
    {
        "type": "order",
        "q": "Arrange the lifecycle of a queue-triggered Container Apps worker with min-replicas 0, from idle to idle.",
        "items": [
            "The app sits at zero replicas, incurring no compute charge",
            "Messages accumulate in the Azure Storage queue",
            "The KEDA scaler detects the queue length exceeds its threshold",
            "Replicas are added, up to the configured max-replicas",
            "The queue drains and the app scales back down to zero",
        ],
        "explain": "This is the scale-to-zero loop: KEDA watches the event source (queue length) rather than replica CPU, so it can wake an app that has zero running replicas. Replicas grow toward max-replicas while backlog persists, then fall away — back to zero and zero cost — once the queue empties. A CPU/memory rule could never start this loop because it needs a replica running to measure.",
    },
]
