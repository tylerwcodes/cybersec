TOPIC = {
    "id": "4.9",
    "domain": 4,
    "title": "Given a scenario, use data sources to support an investigation.",
    "short": "Investigation Data Sources",
}

TERMS = [
    ("Firewall log", "Boundary device record of traffic decisions: source/destination IP and port, protocol, and the action taken (allow/deny)"),
    ("Next-generation firewall (NGFW) log", "Firewall log enriched with application awareness: identified application, URL category, user identity, and threat detections"),
    ("Application log", "Log generated by software itself (web server, database, mail server, custom app) recording requests, transactions, errors, and app-level authentication"),
    ("Web server access log", "Application log listing every HTTP request — source IP, timestamp, URL/query string, status code, user agent; injection attempts appear in the URL"),
    ("Endpoint log", "Host-generated records from workstations/servers: EDR telemetry, antivirus detections, process execution, software installs, USB activity"),
    ("OS-specific security log", "The operating system's own record of security events — logons, failed logons, account changes, privilege use"),
    ("Windows Security log", "Windows event log holding logon/logoff, account, and privilege events; Event ID 4624 = successful logon, 4625 = failed logon"),
    ("Linux authentication log", "/var/log/auth.log (Debian/Ubuntu) or /var/log/secure (RHEL/CentOS); records authentication attempts, sudo use, SSH logins"),
    ("IPS/IDS log", "Record of intrusion detections: matched rule/signature, source/destination, timestamp, and action — alert only (IDS) or block (inline IPS)"),
    ("Network log", "Records from network infrastructure — routers, switches, DNS, DHCP, VPN — plus flow data showing traffic patterns"),
    ("NetFlow", "Cisco-developed flow technology recording conversation metadata (endpoints, ports, times, byte counts) but not packet contents; IPFIX is the IETF standard version"),
    ("DHCP log", "Maps an IP address to a MAC address/hostname at a specific time — turns an IP in other logs into an actual device"),
    ("DNS log", "Records every domain name a host attempted to resolve; reveals lookups of malicious or algorithm-generated domains"),
    ("Metadata", "Data about data — describes a file, message, or session without being its content; e.g., email headers, file timestamps, EXIF"),
    ("Email header", "Message metadata showing the true originating IP and each relay (Received: lines, read bottom-up), timestamps, and message ID"),
    ("EXIF data", "Metadata embedded in image files: camera model, timestamp, and often GPS coordinates of where the photo was taken"),
    ("Vulnerability scan (as evidence)", "Scan results showing what weaknesses existed, where, and for how long — reveals likely entry points and other exposed systems"),
    ("Automated report", "Scheduled, tool-generated summary (e.g., weekly SIEM authentication report) that establishes baselines and trends without manual effort"),
    ("Dashboard", "Real-time, at-a-glance graphical view of security data (charts, counters, status) with drill-down; often the front end of a SIEM"),
    ("Packet capture", "Complete recording of network traffic — headers and payloads — enabling session reconstruction and file extraction; stored as .pcap files"),
    ("Wireshark / tcpdump", "Standard packet capture and analysis tools: Wireshark is the graphical analyzer, tcpdump the command-line capture utility"),
    ("SIEM", "Security Information and Event Management — platform that aggregates, normalizes, and correlates logs from many sources and raises alerts"),
]

QUESTIONS = [
    # --- multiple choice ---
    {
        "type": "mc",
        "q": "An analyst needs to determine whether a workstation's outbound connection attempt to a known-malicious IP address was permitted or denied at the network boundary. Which data source should be checked?",
        "choices": ["Endpoint logs", "Firewall logs", "Application logs", "Vulnerability scans"],
        "answer": 1,
        "explain": "Firewall logs record traffic decisions at the boundary — source/destination IP and port, protocol, and the action taken (allow or deny). Endpoint logs show host activity, application logs show software events, and vulnerability scans show weaknesses, not traffic decisions.",
    },
    {
        "type": "mc",
        "q": "Which set of fields would you expect in a traditional firewall log entry?",
        "choices": [
            "Process name, parent process, and file hash",
            "Source/destination IP, source/destination port, protocol, and action",
            "Matched signature name and CVE reference",
            "Camera model, timestamp, and GPS coordinates",
        ],
        "answer": 1,
        "explain": "A traditional firewall logs the 5-tuple — source IP, destination IP, source port, destination port, protocol — plus the allow/deny action and a timestamp. Process details belong to endpoint logs, signatures to IPS/IDS logs, and camera/GPS data to image EXIF metadata.",
    },
    {
        "type": "mc",
        "q": "A defaced web server's logs show repeated requests containing ' OR 1=1 in the URL query strings. In which log type was this evidence found?",
        "choices": ["OS-specific security logs", "Firewall logs", "Application logs", "IPS/IDS logs"],
        "answer": 2,
        "explain": "Web server access logs are application logs — they record every HTTP request including the URL and query string, so SQL injection input like ' OR 1=1 is visible right in the entries. The OS security log records operating system logons and account events, not the application's request handling.",
    },
    {
        "type": "mc",
        "q": "Investigators want to know whether a user's workstation actually executed the malware attached to a phishing email, and what processes it spawned. Which data source is BEST?",
        "choices": ["Endpoint logs", "Firewall logs", "Network logs", "Automated reports"],
        "answer": 0,
        "explain": "Endpoint logs — EDR telemetry, antivirus detections, process execution records — show what happened on the host itself: whether the file ran, what child processes it created, and what it changed. Firewall and network logs only see traffic that crosses the network.",
    },
    {
        "type": "mc",
        "q": "An analyst reviewing a Windows server finds hundreds of Event ID 4625 entries followed by a single 4624 for the same account. Which log holds these events, and what do they suggest?",
        "choices": [
            "The Application log; a software crash loop",
            "The Security log; a brute-force attack that eventually succeeded",
            "The System log; a failing disk",
            "The Setup log; repeated update failures",
        ],
        "answer": 1,
        "explain": "Event IDs 4625 (failed logon) and 4624 (successful logon) live in the Windows Security event log. Many failures followed by a success on one account is the classic brute-force pattern — the attacker guessed until a credential worked, so that account and host now need investigation.",
    },
    {
        "type": "mc",
        "q": "On a Debian-based Linux server, which file would an investigator examine for SSH login attempts and sudo usage?",
        "choices": ["/var/log/auth.log", "/etc/shadow", "/var/log/dmesg", "/etc/passwd"],
        "answer": 0,
        "explain": "Debian/Ubuntu systems record authentication events — SSH logins, sudo use, failed attempts — in /var/log/auth.log (RHEL/CentOS use /var/log/secure). /etc/shadow and /etc/passwd store account data, not events, and dmesg holds kernel boot/hardware messages.",
    },
    {
        "type": "mc",
        "q": "A sensor monitoring traffic from a SPAN port logs a signature match for a known exploit with action 'alert.' What can the investigator conclude?",
        "choices": [
            "The attack was blocked before reaching the target",
            "The exploit traffic reached the target, and the host must be checked for compromise",
            "The traffic was encrypted and could not be inspected",
            "The event was a false positive and can be closed",
        ],
        "answer": 1,
        "explain": "A sensor on a SPAN port is a passive IDS — it sees a copy of traffic and can only alert, never block. The exploit was therefore delivered, and the target must be investigated for compromise. Only an inline IPS could have dropped the traffic, and its log would show a block action.",
    },
    {
        "type": "mc",
        "q": "Which data source records the specific attack signature that was matched AND whether the malicious traffic was dropped inline?",
        "choices": ["IPS logs", "Firewall logs", "Endpoint logs", "Dashboards"],
        "answer": 0,
        "explain": "IPS logs record the matched rule/signature (often with a CVE reference), source and destination, and the action taken — an inline IPS can block traffic and log that it did. A traditional firewall filters on addresses/ports without signature matching, and dashboards only visualize data from such sources.",
    },
    {
        "type": "mc",
        "q": "An investigator must determine which internal host transferred roughly 10 GB to an external IP overnight. Packet contents are not needed, and no capture was running. Which data source answers the question?",
        "choices": ["Packet captures", "Flow data such as NetFlow", "Vulnerability scans", "Application logs"],
        "answer": 1,
        "explain": "Flow data (NetFlow/IPFIX/sFlow) records conversation metadata — which hosts talked, when, over which ports, and how many bytes — and is compact enough to retain long-term. Packet captures would give content but only exist if capture was running at the time; here it was not.",
    },
    {
        "type": "mc",
        "q": "Logs show attack traffic came from internal IP 10.4.2.77, which is dynamically assigned. Which log source identifies the physical device that held that address at the time?",
        "choices": ["DNS logs", "DHCP logs", "IPS logs", "Vulnerability scan reports"],
        "answer": 1,
        "explain": "DHCP logs record lease assignments, mapping an IP address to a MAC address and hostname for a specific time window — turning an IP found in other logs into an actual device. DNS logs show name resolutions, not address assignments.",
    },
    {
        "type": "mc",
        "q": "Which network log source would reveal that a compromised host repeatedly looked up randomly generated domain names associated with malware command-and-control?",
        "choices": ["DHCP logs", "VPN logs", "DNS logs", "Firewall port-scan logs"],
        "answer": 2,
        "explain": "DNS logs record every name a host tries to resolve, exposing lookups of malicious or algorithm-generated (DGA) domains typical of C2 malware. DHCP logs map IPs to devices, VPN logs track remote connections, and firewalls log connections rather than name resolution.",
    },
    {
        "type": "mc",
        "q": "A phishing email displays the CEO's name as the sender. Which data source reveals the message's true originating mail server?",
        "choices": ["The email body text", "The email header metadata", "Endpoint logs on the CEO's laptop", "The mail server's vulnerability scan"],
        "answer": 1,
        "explain": "Email headers are metadata: the Received: lines (read bottom-up) trace every mail server the message actually passed through, exposing the true originating IP regardless of the spoofed display name. The body is attacker-controlled content and proves nothing about origin.",
    },
    {
        "type": "mc",
        "q": "Investigators recover a photo posted by an attacker and extract the camera model, timestamp, and GPS coordinates of where it was taken. What kind of evidence is this?",
        "choices": ["Packet capture data", "Application log data", "Metadata (EXIF)", "Flow data"],
        "answer": 2,
        "explain": "EXIF data embedded in image files is metadata — data about the photo rather than the picture content itself — and can include camera model, timestamp, and GPS coordinates, placing the device at a location and time.",
    },
    {
        "type": "mc",
        "q": "Which statement BEST defines metadata as an investigative data source?",
        "choices": [
            "The full payload of captured network packets",
            "Data that describes other data, such as timestamps, headers, and authorship details",
            "A real-time graphical summary of security metrics",
            "A scheduled summary document generated by a security tool",
        ],
        "answer": 1,
        "explain": "Metadata is data about data — email headers, file creation/modification times and authors, image EXIF, web user agents, mobile call records. It describes the object without being its content. Payloads are capture content, and the other options describe dashboards and automated reports.",
    },
    {
        "type": "mc",
        "q": "After a server compromise, the security team reviews last quarter's scan results and finds the exploited service had a known critical flaw the whole time. Which data source provided this insight?",
        "choices": ["Vulnerability scans", "Packet captures", "Dashboards", "Endpoint logs"],
        "answer": 0,
        "explain": "Vulnerability scan history shows what weaknesses existed, where, and for how long — here revealing the likely entry point and its exposure window. It also identifies other systems sharing the same flaw that may likewise be compromised. No other listed source records known-vulnerability state over time.",
    },
    {
        "type": "mc",
        "q": "Management wants a summary of authentication failures and malware detections delivered every Monday without any analyst effort. Which data source fits?",
        "choices": ["A dashboard", "An automated report", "A packet capture", "Metadata"],
        "answer": 1,
        "explain": "Automated reports are scheduled, tool-generated summaries — a SIEM can compile and deliver the weekly digest with no manual work, and the accumulated reports double as a historical baseline. A dashboard is a live visual display, not a scheduled document.",
    },
    {
        "type": "mc",
        "q": "A SOC wants continuous at-a-glance visibility of alert volume, failed logons, and traffic anomalies on a wall display, with the ability to click into the underlying events. Which data source is this?",
        "choices": ["An automated report", "A dashboard", "A vulnerability scan", "An IPS log"],
        "answer": 1,
        "explain": "Dashboards are real-time graphical views — charts, counters, and status indicators, typically the front end of a SIEM — built for at-a-glance situational awareness with drill-down into details. An automated report is a periodic generated document, not a live display.",
    },
    {
        "type": "mc",
        "q": "Investigators must determine exactly which customer records were transmitted to the attacker and reconstruct the session byte-for-byte. Which data source can do this?",
        "choices": ["NetFlow records", "Firewall logs", "Packet captures", "DHCP logs"],
        "answer": 2,
        "explain": "Packet captures record complete traffic — headers and payloads — so analysts can reconstruct sessions and extract the actual transferred files. NetFlow and firewall logs only record metadata about the conversation (endpoints, ports, bytes, action), never the content.",
    },
    {
        "type": "mc",
        "q": "Which pair of tools is standard for capturing and analyzing network packets?",
        "choices": ["Nmap and Nessus", "Wireshark and tcpdump", "Metasploit and Burp Suite", "Snort and OpenVAS"],
        "answer": 1,
        "explain": "Wireshark (graphical analyzer) and tcpdump (command-line utility) are the standard packet capture tools, saving traffic to .pcap files. Nmap scans networks, Nessus and OpenVAS scan for vulnerabilities, Snort is an IDS/IPS, and Metasploit/Burp are attack/testing tools.",
    },
    {
        "type": "mc",
        "q": "An analyst opens a packet capture of a suspect session but finds the traffic is TLS-encrypted. What investigative value remains in the capture?",
        "choices": [
            "None — encrypted captures are useless",
            "The full plaintext payload, after the capture tool auto-decrypts it",
            "Connection metadata: endpoints, ports, timing, and data volume",
            "The attacker's private key, recoverable from the handshake",
        ],
        "answer": 2,
        "explain": "Encryption hides the payload, but the capture still shows who talked to whom, when, over which ports, and how much data moved — effectively metadata, which can still prove exfiltration volume and timing. Capture tools cannot decrypt TLS without the session keys, and private keys are not recoverable from a handshake.",
    },
    {
        "type": "mc",
        "q": "Which platform aggregates, normalizes, and correlates log data from firewalls, endpoints, and servers across the enterprise, raising alerts that typically start an investigation?",
        "choices": ["SIEM", "IPS", "NGFW", "EDR"],
        "answer": 0,
        "explain": "A SIEM (Security Information and Event Management) platform centralizes logs from many sources, correlates events across them, and generates alerts and dashboards — it is where investigations usually start before drilling into individual sources. IPS, NGFW, and EDR are single-vantage sources that feed the SIEM.",
    },
    {
        "type": "mc",
        "q": "Which investigative question is answered by OS-specific security logs rather than application logs?",
        "choices": [
            "Which SQL queries hit the database last night?",
            "Which HTTP requests contained directory-traversal strings?",
            "Which accounts were created on the server and who failed to log on?",
            "Which URLs did the proxy block?",
        ],
        "answer": 2,
        "explain": "OS-specific security logs (Windows Security log; Linux auth.log/secure) record operating-system events: logons, failed logons, account creation, privilege use. SQL queries and HTTP requests are recorded by the applications handling them, and blocked URLs appear in proxy/firewall logs.",
    },
    # --- true/false ---
    {
        "type": "tf",
        "q": "True or False: A passively deployed IDS can only alert on malicious traffic, while an inline IPS can block it.",
        "answer": True,
        "explain": "An IDS monitors a copy of traffic (via SPAN port or TAP) and can only detect and alert — the traffic still reaches its destination. An IPS sits inline in the traffic path, so it can drop malicious packets and log the block action.",
    },
    {
        "type": "tf",
        "q": "True or False: NetFlow records include the full payload of every packet in a conversation.",
        "answer": False,
        "explain": "Flow data (NetFlow/IPFIX/sFlow) records conversation metadata only — endpoints, ports, protocol, timestamps, and byte/packet counts. Payloads are found only in packet captures; that trade-off is exactly why flow data is compact enough for long retention.",
    },
    {
        "type": "tf",
        "q": "True or False: The Received: lines in an email header, read from bottom to top, trace the mail servers a message actually passed through.",
        "answer": True,
        "explain": "Each relaying mail server prepends its own Received: line, so reading them bottom-up reconstructs the delivery path from origin to destination and exposes the true originating IP — regardless of what the spoofed From display claims.",
    },
    {
        "type": "tf",
        "q": "True or False: Dashboards are designed primarily for scheduled delivery of historical summary documents.",
        "answer": False,
        "explain": "That describes automated reports. Dashboards are real-time, at-a-glance graphical displays of current status with drill-down capability — they trigger investigations, while automated reports are scheduled, generated documents that establish baselines and trends.",
    },
    {
        "type": "tf",
        "q": "True or False: On Windows, successful and failed logon attempts are recorded in the Security event log.",
        "answer": True,
        "explain": "The Windows Security event log holds authentication and account events — Event ID 4624 for a successful logon, 4625 for a failed one, plus account changes and privilege use. The Application and System logs cover software and OS-component events instead.",
    },
    {
        "type": "tf",
        "q": "True or False: Full packet captures require far more storage than flow records covering the same traffic.",
        "answer": True,
        "explain": "Captures store every byte — headers and payloads — so they are enormous, and few organizations can capture everything continuously. Flow records summarize each conversation in a small fixed record, which is why flow data covers the long term and captures are targeted.",
    },
    # --- fill in ---
    {
        "type": "fill",
        "q": "On Windows, logon events, account changes, and privilege use are recorded in the ______ event log. (type your answer)",
        "answers": ["security", "the security log", "security log", "windows security"],
        "explain": "The Windows Security event log is the OS-specific security log: it records logon/logoff (4624/4625), account creation and changes, and privilege use. The Application log records software events and the System log records OS component events.",
    },
    {
        "type": "fill",
        "q": "Data that describes other data — such as email headers, file timestamps, and EXIF fields — is called ______. (type your answer)",
        "answers": ["metadata", "meta data", "meta-data"],
        "explain": "Metadata is data about data: it describes a file, message, photo, or session without being its content. Investigators use it to establish origin (email headers), timelines (timestamps), authorship (document properties), and location (EXIF GPS).",
    },
    {
        "type": "fill",
        "q": "tcpdump and Wireshark are the standard tools for performing a packet ______. (type your answer)",
        "answers": ["capture", "captures", "capturing", "cap"],
        "explain": "Packet captures record complete network traffic — headers and payloads — into .pcap files. tcpdump captures from the command line; Wireshark captures and provides deep graphical analysis, including session reconstruction and file extraction.",
    },
    {
        "type": "fill",
        "q": "The platform that aggregates and correlates log data from across the enterprise and raises alerts is known by the abbreviation ______. (type your answer)",
        "answers": ["siem", "security information and event management"],
        "explain": "A SIEM (Security Information and Event Management) system centralizes logs from firewalls, endpoints, servers, and network devices, normalizes and correlates them, and drives the alerts, dashboards, and automated reports investigations rely on.",
    },
    {
        "type": "fill",
        "q": "The Cisco-developed technology that records network conversation metadata — who talked to whom, when, and how many bytes — is called ______. (type your answer)",
        "answers": ["netflow", "net flow"],
        "explain": "NetFlow records flow metadata: source/destination, ports, protocol, timestamps, and byte counts, with no payloads. IPFIX is the IETF standard derived from it, and sFlow is a sampling-based alternative — all answer 'who talked to whom and how much.'",
    },
    # --- matching ---
    {
        "type": "match",
        "q": "Match each log type to the evidence it provides.",
        "pairs": [
            ["Firewall logs", "Whether boundary traffic was allowed or denied"],
            ["Application logs", "HTTP requests showing injected SQL in URL strings"],
            ["Endpoint logs", "Process execution and USB activity on a host"],
            ["OS-specific security logs", "Logon attempts and account changes"],
            ["IPS/IDS logs", "Matched attack signatures and alert/block actions"],
        ],
        "explain": "Each log sees a different vantage: firewalls log traffic decisions at the boundary, applications log their own requests and errors, endpoint tools log host activity, the OS logs authentication and accounts, and IPS/IDS sensors log signature matches with the action taken.",
    },
    {
        "type": "match",
        "q": "Match each data source to its description.",
        "pairs": [
            ["Vulnerability scans", "Show what known weaknesses existed and where"],
            ["Automated reports", "Scheduled tool-generated summaries establishing baselines"],
            ["Dashboards", "Real-time at-a-glance visual status with drill-down"],
            ["Packet captures", "Full traffic contents enabling session reconstruction"],
            ["Flow data", "Conversation metadata: endpoints, times, and byte counts"],
        ],
        "explain": "Scans reveal pre-existing weaknesses; automated reports summarize on a schedule; dashboards visualize live status; packet captures preserve every byte on the wire; flow data records who talked to whom and how much — without content.",
    },
    {
        "type": "match",
        "q": "Match each metadata source to an artifact it can yield.",
        "pairs": [
            ["Email", "Received: header lines exposing the originating server"],
            ["File", "Author and creation/modification timestamps"],
            ["Image", "EXIF GPS coordinates of where a photo was taken"],
            ["Web", "Browser user-agent strings and cookies"],
            ["Mobile", "Call records showing who called whom and when"],
        ],
        "explain": "Metadata describes the object rather than its content: email headers trace delivery paths, file properties record authorship and timing, EXIF embeds camera and location data, web metadata identifies browsers and sessions, and mobile records show call/SMS activity — not what was said.",
    },
    # --- ordering ---
    {
        "type": "order",
        "q": "Arrange the steps of a phishing investigation in the order the data sources would logically be used.",
        "items": [
            "A user reports a suspicious email to the SOC",
            "Examine the email header metadata to find the true originating server",
            "Review endpoint logs to see whether the attachment executed on the workstation",
            "Check firewall and DNS logs for connections from that host to the attacker's domain",
            "Pull packet captures of the suspect traffic to determine exactly what data left",
            "Document the findings and timeline in the investigation report",
        ],
        "explain": "The investigation narrows step by step: the report triggers it, header metadata establishes the email's true origin, endpoint logs confirm execution on the host, firewall/DNS logs confirm outbound contact, packet captures reveal what content actually left, and documentation closes out the timeline.",
    },
]
